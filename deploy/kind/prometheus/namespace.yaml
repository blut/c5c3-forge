# SPDX-FileCopyrightText: Copyright 2026 SAP SE or an SAP affiliate company
#
# SPDX-License-Identifier: Apache-2.0

# kind-only opt-in monitoring Namespace for kube-prometheus-stack,
# relocated to this overlay rather than deploy/flux-system/namespaces.yaml
# because the production stack does NOT install kube-prometheus-stack
# (CC-0100, REQ-001 — production overlays are unchanged).
---
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
