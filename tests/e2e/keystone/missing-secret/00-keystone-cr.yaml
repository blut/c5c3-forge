# SPDX-FileCopyrightText: Copyright 2026 SAP SE or an SAP affiliate company
#
# SPDX-License-Identifier: Apache-2.0

# Keystone CR fixture for the missing-secret E2E test (CC-0016).
# References non-existent Secrets (missing-secret-keystone-db,
# missing-secret-keystone-admin) to test SecretsReady=False recovery.
# Uses managed mode (clusterRef) so the MariaDB User CR is created
# automatically with the correct username derived from keystone.Name.
apiVersion: keystone.openstack.c5c3.io/v1alpha1
kind: Keystone
metadata:
  name: keystone-missing-secret
  namespace: openstack
spec:
  replicas: 1
  image:
    repository: ghcr.io/c5c3/keystone
    tag: "2025.2"
  database:
    clusterRef:
      name: openstack-db
    database: keystone_missing
    secretRef:
      name: missing-secret-keystone-db
  cache:
    clusterRef:
      name: openstack-memcached
    backend: dogpile.cache.pymemcache
  fernet:
    rotationSchedule: "0 0 * * 0"
    maxActiveKeys: 3
  bootstrap:
    adminUser: admin
    adminPasswordSecretRef:
      name: missing-secret-keystone-admin
    region: RegionOne
