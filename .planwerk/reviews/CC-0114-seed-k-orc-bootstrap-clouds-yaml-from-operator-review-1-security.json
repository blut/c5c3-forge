{"summary": "Security review of CC-0114 (move K-ORC bootstrap clouds.yaml seed from deploy-infra shell into the c5c3-operator). All focus areas are clean. The admin password is read from a Secret, materialised only into Secrets, and pushed via ESO PushSecret; it is never logged and never placed in a process argument — a genuine improvement over the retired in-pod `sh -c` shell seed. buildPasswordCloudsYAML embeds the password via Go %q, which double-quotes/escapes newlines and quotes and thus prevents YAML breakout (the old shell seed used an unquoted heredoc). Owner references are same-namespace only (childNamespace(cp)==cp.Namespace, SetControllerReference). The per-CR remoteRef.key (adminAppCredentialRemoteKeyFor: openstack/keystone/{ns}/{name}/admin/app-credential) is scoped by both namespace AND name, so one ControlPlane cannot read another's OpenBao path. Removing the shell mark_eso_managed for the app-credential path is correct: the path is no longer shell-pre-seeded, so it starts non-existent and ESO creates+stamps its own managed-by marker on first push (no guard collision). No new RBAC is added; the existing external-secrets.io/externalsecrets marker (controlplane_controller.go:82) and Helm ClusterRole (_helpers.tpl:199) already cover the esov1 ExternalSecret, and esov1 is registered in main.go:51.", "verdict": "APPROVED",
 "tests_checklist": [{"item":"Unit tests assert seedBootstrapCloudsYAML renders a password-based clouds.yaml (contains 'password:') write-if-empty and re-seeds after key deletion","checked":true},{"item":"Integration tests assert the operator (not the test) creates the ES, with correct per-CR remoteRef.key, single Data entry, and ControlPlane controller-ownership","checked":true},{"item":"Test asserts no PushSecret/ExternalSecret is created before the admin password Secret exists (defer path)","checked":true}],
 "code_quality_checklist": [{"item":"Each new step in reconcileKORC guarded with a distinct KORCReady=False Reason (SeedCloudsYamlError/PushSecretError/ExternalSecretError)","checked":true},{"item":"Error wraps use %w and reference object name, not secret contents","checked":true}],
 "security_checklist": [{"item":"Admin password never logged (all log/error messages use %v of an error or static strings; no password interpolation)","checked":true},{"item":"Admin password never passed as a process/command-line argument (rendered in-process to []byte, written to a Secret)","checked":true},{"item":"YAML injection prevented: password embedded via Go %q (double-quoted, escapes newlines/quotes); improvement over old unquoted shell heredoc","checked":true},{"item":"No cross-namespace owner references: all owned objects in childNamespace(cp)==cp.Namespace, SetControllerReference is same-namespace","checked":true},{"item":"Per-CR OpenBao path scoped by namespace AND name (adminAppCredentialRemoteKeyFor) — one ControlPlane cannot read another's credential path","checked":true},{"item":"ESO managed-by: removal of shell mark_eso_managed for app-credential path is correct (path no longer pre-seeded; ESO PushSecret creates+marks it itself)","checked":true},{"item":"No new RBAC markers added; existing externalsecrets RBAC marker and Helm ClusterRole already cover esov1 ExternalSecret (same external-secrets.io group)","checked":true},{"item":"esov1 (external-secrets.io/v1) registered in runtime scheme (main.go:51)","checked":true},{"item":"Write-if-empty seed never clobbers a minted credential-based clouds.yaml and never touches the 'value' key","checked":true}],
 "architecture_checklist": [{"item":"Seed/Push/ES steps placed after ensureAppCredentialSecret and before the AC re-mint decision; ordering documented and convergence-safe (K-ORC retries auth async)","checked":true},{"item":"ExternalSecret is esov1; PushSecret stays esov1alpha1 (intentional, documented)","checked":true}],
 "dry_yagni_checklist": [{"item":"Per-CR path formula centralised in adminAppCredentialRemoteKeyFor and reused by both PushSecret writer and ExternalSecret reader","checked":true}],
 "fail_fast_checklist": [{"item":"Missing admin password defers with WaitingForAdminPassword BEFORE any seed/push/ES create (verified by TestReconcileKORC_DefersBeforeSeedWhenAdminPasswordMissing)","checked":true}],
 "defensive_checklist": [{"item":"secret.Data nil-guarded before write; SecretName falls back to korcCloudsYamlSecretName on empty spec (webhook-bypass edge)","checked":true}],
 "security_findings": [],
 "architecture_findings": [],
 "issues_found": [],
 "suggested_improvements": ["Optional hardening note (not a defect): Go %q and YAML double-quoted-scalar escape grammars diverge for some non-printable/control characters (e.g. certain \\xHH/\\uHHHH forms). For the current threat model this is moot because the admin password originates from a controlled Secret and is a hex string in practice, but if AdminCredential.PasswordSecretRef could ever carry arbitrary bytes, consider rendering clouds.yaml via a YAML marshaller (e.g. sigs.k8s.io/yaml) instead of fmt.Sprintf+%q to guarantee escape-grammar correctness for all byte values.", "On an upgraded cluster a pre-CC-0114 deploy may have already run `bao kv put` on the per-CR app-credential path; since the operator's PushSecret already targeted it before this change, it should already be ESO-managed, so no migration action is required — worth a one-line note in the upgrade docs for operators reasoning about the managed-by marker."],
 "next_steps": [], "detected_patterns": []}
