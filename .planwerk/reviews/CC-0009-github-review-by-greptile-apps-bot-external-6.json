{
  "feature_id": "CC-0009",
  "title": "GitHub Review by greptile-apps[bot]",
  "summary": "1. **Set explicit TLS certificate lifetime** on the OpenBao Certificate resource (`duration`/`renewBefore`) to avoid silent 90-day rotations that cause Raft TLS handshake failures since OpenBao doesn't hot-reload certs — or add a reloader to restart pods on Secret change.\n2. **Configure or fix RBAC for the management cluster Kubernetes auth mount** — `setup-auth.sh` enables the mount and creates a role but never calls `bao write auth/kubernetes/management/config`, and no `system:auth-delegator` ClusterRoleBinding exists, so ESO token validation will fail with 403. Also update the docs that claim it's \"fully configured.\"\n3. **Quote non-generated values** in `write-bootstrap-secrets.sh` when building `put_args` to prevent shell injection/misparsing, and **replace deprecated `policies`/`ttl` with `token_policies`/`token_ttl`** in the Kubernetes auth role writes in `setup-auth.sh` to match the AppRole style and avoid future breakage.",
  "verdict": "ADDRESSED",
  "tests_checklist": [],
  "code_quality_checklist": [],
  "security_checklist": [],
  "architecture_checklist": [],
  "dry_yagni_checklist": [],
  "fail_fast_checklist": [],
  "defensive_checklist": [],
  "security_findings": [],
  "architecture_findings": [],
  "issues_found": [],
  "suggested_improvements": [],
  "next_steps": [],
  "reviewer": "greptile-apps[bot]",
  "date": "2026-03-12T09:32:22Z",
  "commit_reference": "",
  "review_type": "external",
  "sequence_number": 6,
  "external_feedback": "",
  "code_comments": [
    {
      "body": "**Certificate `duration` not set — defaults to 90-day rotation cycle**\n\nNo `duration` (or `renewBefore`) field is set on the Certificate. cert-manager defaults to a 90-day lifetime with automatic renewal 30 days before expiry. For a production HA Raft cluster this means the `openbao-tls` Secret will be rotated roughly every 60–70 days.\n\nOpenBao does not hot-reload TLS certificates from the filesystem when the Secret is updated — all three pods will continue serving the old certificate until they are restarted. If `cert-manager` rotates the Secret and the pods are not restarted, the on-disk certificate diverges from the in-memory one, which can cause Raft peer TLS handshake failures between newly-started and already-running pods.\n\nConsider setting an explicit `duration` (e.g., `8760h` for 1 year) and `renewBefore` to reduce rotation frequency and operational surprise:\n\n```yaml\nspec:\n  secretName: openbao-tls\n  duration: 8760h\n  renewBefore: 720h\n  issuerRef:\n    ...\n```\n\nAlternatively, add a restart annotation or a cert-manager `Certificate` rotation hook (via `reloader`/`stakater`) to restart the StatefulSet when the Secret changes.\n\n<details><summary>Prompt To Fix With AI</summary>\n\n`````markdown\nThis is a comment left during a code review.\nPath: deploy/flux-system/infrastructure/openbao-tls-cert.yaml\nLine: 11-25\n\nComment:\n**Certificate `duration` not set — defaults to 90-day rotation cycle**\n\nNo `duration` (or `renewBefore`) field is set on the Certificate. cert-manager defaults to a 90-day lifetime with automatic renewal 30 days before expiry. For a production HA Raft cluster this means the `openbao-tls` Secret will be rotated roughly every 60–70 days.\n\nOpenBao does not hot-reload TLS certificates from the filesystem when the Secret is updated — all three pods will continue serving the old certificate until they are restarted. If `cert-manager` rotates the Secret and the pods are not restarted, the on-disk certificate diverges from the in-memory one, which can cause Raft peer TLS handshake failures between newly-started and already-running pods.\n\nConsider setting an explicit `duration` (e.g., `8760h` for 1 year) and `renewBefore` to reduce rotation frequency and operational surprise:\n\n```yaml\nspec:\n  secretName: openbao-tls\n  duration: 8760h\n  renewBefore: 720h\n  issuerRef:\n    ...\n```\n\nAlternatively, add a restart annotation or a cert-manager `Certificate` rotation hook (via `reloader`/`stakater`) to restart the StatefulSet when the Secret changes.\n\nHow can I resolve this? If you propose a fix, please make it concise.\n`````\n</details>",
      "path": "deploy/flux-system/infrastructure/openbao-tls-cert.yaml",
      "line": 25,
      "author": "greptile-apps[bot]",
      "created_at": "2026-03-12T09:32:18Z",
      "id": null,
      "resolution": "Added `duration: 8760h` and `renewBefore: 720h` fields to the Certificate spec in deploy/flux-system/infrastructure/openbao-tls-cert.yaml (lines 18-22), with a comment explaining that the 1-year lifetime reduces rotation frequency because OpenBao does not hot-reload TLS certificates."
    },
    {
      "body": "**Management cluster Kubernetes auth mount is enabled but never configured**\n\n`setup-auth.sh` enables the `kubernetes/management` auth mount and creates the `eso-management` role, but never calls `bao write auth/kubernetes/management/config` to tell OpenBao how to validate service account tokens from the management cluster.\n\nWithout this config call, OpenBao must rely on in-cluster auto-discovery: it uses the pod's own mounted service account token at `/var/run/secrets/kubernetes.io/serviceaccount/token` to make `TokenReview` API calls. This auto-discovery path works **only if** the OpenBao service account holds the `system:auth-delegator` ClusterRole (or equivalent `authentication.k8s.io/tokenreviews: create` permission). That ClusterRoleBinding is not created anywhere in this PR.\n\nIf the OpenBao Helm chart does not create this ClusterRoleBinding by default, every ESO authentication attempt against `kubernetes/management` will fail with a `403` from the Kubernetes TokenReview API — meaning no `ExternalSecret` will be reconciled and no Kubernetes-backed Secret will be created.\n\nThe fix is to either:\n1. Add a `bao write auth/kubernetes/management/config kubernetes_host=\"https://kubernetes.default.svc\" ...` call using the in-pod CA cert, or\n2. Add a `ClusterRoleBinding` binding the OpenBao service account to `system:auth-delegator`.\n\nThe documentation comment at `docs/reference/infrastructure/openbao-bootstrap.md:267` states \"The management cluster mount is **fully configured**\", but that note is only accurate if the Helm chart automatically grants `system:auth-delegator`. This should be verified and the documentation updated to clarify the RBAC prerequisite.\n\n<details><summary>Prompt To Fix With AI</summary>\n\n`````markdown\nThis is a comment left during a code review.\nPath: deploy/openbao/bootstrap/setup-auth.sh\nLine: 53-56\n\nComment:\n**Management cluster Kubernetes auth mount is enabled but never configured**\n\n`setup-auth.sh` enables the `kubernetes/management` auth mount and creates the `eso-management` role, but never calls `bao write auth/kubernetes/management/config` to tell OpenBao how to validate service account tokens from the management cluster.\n\nWithout this config call, OpenBao must rely on in-cluster auto-discovery: it uses the pod's own mounted service account token at `/var/run/secrets/kubernetes.io/serviceaccount/token` to make `TokenReview` API calls. This auto-discovery path works **only if** the OpenBao service account holds the `system:auth-delegator` ClusterRole (or equivalent `authentication.k8s.io/tokenreviews: create` permission). That ClusterRoleBinding is not created anywhere in this PR.\n\nIf the OpenBao Helm chart does not create this ClusterRoleBinding by default, every ESO authentication attempt against `kubernetes/management` will fail with a `403` from the Kubernetes TokenReview API — meaning no `ExternalSecret` will be reconciled and no Kubernetes-backed Secret will be created.\n\nThe fix is to either:\n1. Add a `bao write auth/kubernetes/management/config kubernetes_host=\"https://kubernetes.default.svc\" ...` call using the in-pod CA cert, or\n2. Add a `ClusterRoleBinding` binding the OpenBao service account to `system:auth-delegator`.\n\nThe documentation comment at `docs/reference/infrastructure/openbao-bootstrap.md:267` states \"The management cluster mount is **fully configured**\", but that note is only accurate if the Helm chart automatically grants `system:auth-delegator`. This should be verified and the documentation updated to clarify the RBAC prerequisite.\n\nHow can I resolve this? If you propose a fix, please make it concise.\n`````\n</details>",
      "path": "deploy/openbao/bootstrap/setup-auth.sh",
      "line": 56,
      "author": "greptile-apps[bot]",
      "created_at": "2026-03-12T09:32:19Z",
      "id": null,
      "resolution": "Added an explicit `bao write auth/kubernetes/management/config` call in deploy/openbao/bootstrap/setup-auth.sh (lines 58-69) that configures the management cluster auth mount with `kubernetes_host=\"https://kubernetes.default.svc\"` and `kubernetes_ca_cert=@/var/run/secrets/kubernetes.io/serviceaccount/ca.crt`. Updated the documentation at docs/reference/infrastructure/openbao-bootstrap.md (lines 267-272) to accurately describe the explicit configuration and clarify the `system:auth-delegator` RBAC prerequisite."
    },
    {
      "body": "**Unquoted non-generated values in `put_args` are fragile**\n\nWhen a key-value argument does NOT use the `@generate` marker, it is appended to `put_args` verbatim without quoting:\n\n```bash\nput_args+=\" ${arg}\"\n```\n\nThis is then interpolated directly into the `sh -c` command string:\n\n```bash\nsh -c \"bao kv put '${kv_path}' ${put_args}\"\n```\n\nFor the current callers (`username=keystone`) this is harmless because the value contains no shell metacharacters. However if any future caller passes a value with spaces, equals signs in the value, or shell metacharacters (e.g., `description=my service account`), the shell command will be misparsed and either fail silently or inject unintended shell tokens.\n\nThe generated-password branch already uses proper quoting inside the expanded command. It would be safer to quote non-generated values with single quotes when building `put_args`:\n\n```bash\nput_args+=\" ${key}='${val}'\"\n```\n\n(Values that might contain single quotes would need additional escaping, but that's an edge case not present in today's callers.)\n\n<details><summary>Prompt To Fix With AI</summary>\n\n`````markdown\nThis is a comment left during a code review.\nPath: deploy/openbao/bootstrap/write-bootstrap-secrets.sh\nLine: 49-63\n\nComment:\n**Unquoted non-generated values in `put_args` are fragile**\n\nWhen a key-value argument does NOT use the `@generate` marker, it is appended to `put_args` verbatim without quoting:\n\n```bash\nput_args+=\" ${arg}\"\n```\n\nThis is then interpolated directly into the `sh -c` command string:\n\n```bash\nsh -c \"bao kv put '${kv_path}' ${put_args}\"\n```\n\nFor the current callers (`username=keystone`) this is harmless because the value contains no shell metacharacters. However if any future caller passes a value with spaces, equals signs in the value, or shell metacharacters (e.g., `description=my service account`), the shell command will be misparsed and either fail silently or inject unintended shell tokens.\n\nThe generated-password branch already uses proper quoting inside the expanded command. It would be safer to quote non-generated values with single quotes when building `put_args`:\n\n```bash\nput_args+=\" ${key}='${val}'\"\n```\n\n(Values that might contain single quotes would need additional escaping, but that's an edge case not present in today's callers.)\n\nHow can I resolve this? If you propose a fix, please make it concise.\n`````\n</details>",
      "path": "deploy/openbao/bootstrap/write-bootstrap-secrets.sh",
      "line": 63,
      "author": "greptile-apps[bot]",
      "created_at": "2026-03-12T09:32:20Z",
      "id": null,
      "resolution": "Changed the non-generated value branch in deploy/openbao/bootstrap/write-bootstrap-secrets.sh (line 58) from `put_args+=\" ${arg}\"` to `put_args+=\" ${key}='${val}'\"`, adding single-quote wrapping to guard against shell metacharacters. Added a comment (lines 56-57) explaining the quoting rationale."
    },
    {
      "body": "**Deprecated `policies` and `ttl` parameter names in Kubernetes auth roles**\n\nThe Kubernetes auth role writes use the deprecated `policies` and `ttl` field names. The AppRole role write at line 77 correctly uses the current `token_policies` and `token_ttl` names. In OpenBao (and Vault ≥1.2), `policies` and `ttl` are accepted as backward-compat aliases only — the canonical parameter names are the `token_*` prefixed variants.\n\nThis is an inconsistency within the same script that could confuse operators, and the aliases may be removed in a future OpenBao release. The four Kubernetes auth role writes should use `token_policies` and `token_ttl` to match the AppRole style.\n\n<details><summary>Prompt To Fix With AI</summary>\n\n`````markdown\nThis is a comment left during a code review.\nPath: deploy/openbao/bootstrap/setup-auth.sh\nLine: 61-67\n\nComment:\n**Deprecated `policies` and `ttl` parameter names in Kubernetes auth roles**\n\nThe Kubernetes auth role writes use the deprecated `policies` and `ttl` field names. The AppRole role write at line 77 correctly uses the current `token_policies` and `token_ttl` names. In OpenBao (and Vault ≥1.2), `policies` and `ttl` are accepted as backward-compat aliases only — the canonical parameter names are the `token_*` prefixed variants.\n\nThis is an inconsistency within the same script that could confuse operators, and the aliases may be removed in a future OpenBao release. The four Kubernetes auth role writes should use `token_policies` and `token_ttl` to match the AppRole style.\n\nHow can I resolve this? If you propose a fix, please make it concise.\n`````\n</details>",
      "path": "deploy/openbao/bootstrap/setup-auth.sh",
      "line": 67,
      "author": "greptile-apps[bot]",
      "created_at": "2026-03-12T09:32:21Z",
      "id": null,
      "resolution": "Updated all four Kubernetes auth role writes in deploy/openbao/bootstrap/setup-auth.sh (lines 77-78) from deprecated `policies` and `ttl` to the canonical `token_policies` and `token_ttl` parameter names, making them consistent with the AppRole role write in the same script."
    }
  ],
  "resolution_summary": "All four review comments were addressed. The TLS certificate was given an explicit 1-year duration and renewBefore window. The management cluster Kubernetes auth mount now has explicit config with kubernetes_host and CA cert. Non-generated values in write-bootstrap-secrets.sh are now single-quoted to prevent shell metacharacter issues. Kubernetes auth role writes were updated from deprecated `policies`/`ttl` to canonical `token_policies`/`token_ttl`. The documentation at docs/reference/infrastructure/openbao-bootstrap.md lines 267-272 was also updated to accurately describe the explicit management cluster auth configuration and its RBAC prerequisite (system:auth-delegator), which was not directly called out in any single comment but was needed to keep docs consistent with the setup-auth.sh changes from comments [1] and [3].",
  "github_review_id": 3935128917
}