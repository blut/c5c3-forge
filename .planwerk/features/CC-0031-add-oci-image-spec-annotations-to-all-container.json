{
  "feature_id": "CC-0031",
  "title": "Add OCI Image Spec annotations to all container images",
  "slug": "add-oci-image-spec-annotations-to-all-container",
  "status": "proposed",
  "phase": null,
  "summary": "",
  "description": "**Size:** 🔧 small\n**Category:** infrastructure\n**Priority:** medium\n**Source:** Proposed for 'Currently no OCI labels are being set on the container'\n\nAdd standard OCI annotations to python-base, venv-builder, and keystone container images using a two-layer approach:\n\n1. **CI-generated labels via `docker/metadata-action@v5`** (3 new steps in `.github/workflows/build-images.yaml`):\n   - `meta-python-base` step in `build-base-images` job before `build-python-base` step, generating OCI labels (title, description, source, url, version, created, revision, licenses, vendor)\n   - `meta-venv-builder` step in `build-base-images` job before `build-venv-builder` step, same label set\n   - `meta-keystone` step in `build-service-images` job before `build-service` step, with `version` overridden to `${{ steps.source-ref.outputs.ref }}` (upstream release version from source-refs.yaml) via `raw` version strategy\n   - Wire each metadata step's `labels` output into the corresponding `build-push-action` step via `labels: ${{ steps.<meta-step-id>.outputs.labels }}`\n\n2. **Static `LABEL` instructions in Dockerfiles** (3 files):\n   - Add `org.opencontainers.image.title`, `.description`, `.licenses=Apache-2.0`, `.vendor` as static LABELs to `images/python-base/Dockerfile`, `images/venv-builder/Dockerfile`, and `images/keystone/Dockerfile`\n   - These provide baseline metadata for local builds; CI-generated labels override them at push time\n\nExcluded: tag logic changes, custom/vendor-specific labels, base.name/base.digest annotations, Makefile changes, verify job changes.\n\n**Rationale:** Container images currently ship with zero metadata labels. This makes it impossible to trace a running image back to its source commit, repository, license, or build timestamp without external tooling. OCI annotations are the industry standard adopted by registries (Docker Hub, GHCR, Quay), vulnerability scanners, and audit tools. Adding them improves supply chain transparency, simplifies incident response (which commit produced this image?), and aligns with container best practices at minimal cost.\n\n**Affected Areas:**\n- .github/workflows/build-images.yaml\n- images/python-base/Dockerfile\n- images/venv-builder/Dockerfile\n- images/keystone/Dockerfile",
  "stories": [],
  "requirements": [],
  "tasks": [],
  "test_specifications": [],
  "affected_files": [],
  "similar_patterns": [],
  "review_criteria": [],
  "implementation_notes": "",
  "status_history": {
    "proposed": {
      "github_account": "berendt",
      "timestamp": "2026-03-08T22:55:00.113179"
    }
  },
  "elaboration": "**Size:** 🔧 small\n**Category:** infrastructure\n**Priority:** medium\n\n## Summary\n\nAdd [OCI Image Spec annotations](https://specs.opencontainers.org/image-spec/annotations/) to all container images (`python-base`, `venv-builder`, `keystone`). Standard labels — title, description, source URL, version, creation timestamp, Git revision, license, and vendor — will be generated dynamically in CI using `docker/metadata-action` and supplemented with static labels in Dockerfiles for local build visibility.\n\n## Scope\n\n**Included:**\n- Add `docker/metadata-action` steps to `build-images.yaml`: two in `build-base-images` (one for `python-base`, one for `venv-builder`) and one in `build-service-images` (for `keystone` / matrix services)\n- Generate standard OCI annotations: `title`, `description`, `url`, `source`, `version`, `created`, `revision`, `licenses`, `vendor`, `documentation`\n- Wire metadata-action `labels` output into the existing `docker/build-push-action` steps (`build-python-base`, `build-venv-builder`, `build-service`) via their `labels` input\n- Add static `LABEL` instructions to all three Dockerfiles (`images/python-base/Dockerfile`, `images/venv-builder/Dockerfile`, `images/keystone/Dockerfile`) for `title`, `description`, `licenses`, `vendor`\n- For `keystone`: override `org.opencontainers.image.version` with the upstream release version from `releases/<release>/source-refs.yaml` (already resolved as `steps.source-ref.outputs.ref` / `VERSION` in the workflow)\n\n**Excluded:**\n- Replacing the existing tag derivation logic (composite/version/SHA tags remain as-is) — YAGNI, tags work correctly today\n- Custom or vendor-specific labels beyond the OCI standard set — YAGNI, no current consumer\n- Adding `org.opencontainers.image.base.name` / `.base.digest` annotations — would require cross-job plumbing of base image digests; can be a follow-up if needed\n- Modifying the `Makefile` `docker-build` target (it's a stub that errors out)\n- Changes to `verify-base-images` or `verify-service-images` jobs — they pull by digest/tag, not affected by labels\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph job1[\"build-base-images job\"]\n        direction TB\n        meta1[\"docker/metadata-action<br/>python-base labels\"]\n        build1[\"build-push-action<br/>#build-python-base\"]\n        meta1 -->|\"labels output\"| build1\n\n        meta2[\"docker/metadata-action<br/>venv-builder labels\"]\n        build2[\"build-push-action<br/>#build-venv-builder\"]\n        meta2 -->|\"labels output\"| build2\n    end\n\n    subgraph job2[\"build-service-images job\"]\n        direction TB\n        meta3[\"docker/metadata-action<br/>keystone labels\"]\n        build3[\"build-push-action<br/>#build-service\"]\n        meta3 -->|\"labels output\"| build3\n    end\n\n    subgraph sources[\"Label Sources\"]\n        gh[\"GitHub Context<br/>sha, repo URL, timestamp\"]\n        df[\"Dockerfile static LABELs<br/>title, description, license, vendor\"]\n        rel[\"source-refs.yaml<br/>upstream version\"]\n    end\n\n    gh --> meta1\n    gh --> meta2\n    gh --> meta3\n    rel -->|\"version override\"| meta3\n    df --> build1\n    df --> build2\n    df --> build3\n\n    subgraph output[\"Pushed Images\"]\n        oci[\"OCI Annotations<br/>on manifest + config\"]\n    end\n\n    build1 --> oci\n    build2 --> oci\n    build3 --> oci\n```\n\n## Key Components\n\n- **`docker/metadata-action` steps** (3 new steps): one before each `build-push-action` — generates OCI-compliant labels from GitHub Actions context; each configured with image-specific `title` and `description`; keystone step additionally overrides `version` from `steps.source-ref.outputs.ref`\n- **`build-push-action` label wiring** (3 existing steps modified): add `labels: ${{ steps.<meta-step-id>.outputs.labels }}` to `build-python-base`, `build-venv-builder`, and `build-service`\n- **Dockerfile `LABEL` instructions** (3 files modified): static `org.opencontainers.image.title`, `.description`, `.licenses`, `.vendor` in `images/python-base/Dockerfile`, `images/venv-builder/Dockerfile`, `images/keystone/Dockerfile` — CI-generated labels override these at push time\n- **Keystone version override**: `docker/metadata-action` for keystone uses a `raw` version strategy set to `${{ steps.source-ref.outputs.ref }}` (e.g. `28.0.0`) instead of the default Git-derived version"
}