{
  "feature_id": "CC-0017",
  "title": "H001: Keystone Operator Packaging",
  "slug": "h001-keystone-operator-packaging",
  "status": "proposed",
  "phase": null,
  "summary": "",
  "description": "**Size:** 🔨 medium\n**Category:** infrastructure\n**Priority:** high\n\n## Open Questions\n\nAll questions resolved by user decisions. No remaining ambiguities.\n\n## Summary\n\nCreate the packaging layer for the Keystone operator: a multi-stage Dockerfile (Go 1.25 builder → distroless runtime), a Helm chart with all deployment manifests (Deployment, Service, ServiceAccount, RBAC, webhook configs, CRDs), and a FluxCD HelmRelease to deploy it via GitOps. This connects the existing operator code (`operators/keystone/`) to the existing FluxCD deployment pipeline (`deploy/flux-system/`), completing Phase 8 (S017) of the project plan.\n\n## Scope\n\n**Included:**\n- `operators/keystone/Dockerfile` — multi-stage build (golang:1.25 builder, gcr.io/distroless/static:nonroot runtime, CGO_ENABLED=0, UID 65532). Build context must be workspace root to resolve `go.work` and `internal/common` dependency.\n- `operators/keystone/helm/keystone-operator/Chart.yaml` — apiVersion v2, chart metadata\n- `operators/keystone/helm/keystone-operator/values.yaml` — image, replicas (default 2), resources, leaderElection (default true), webhook, metrics, serviceAccount\n- `operators/keystone/helm/keystone-operator/crds/keystone.openstack.c5c3.io_keystones.yaml` — CRD copied from `config/crd/bases/`\n- `operators/keystone/helm/keystone-operator/templates/deployment.yaml`\n- `operators/keystone/helm/keystone-operator/templates/service.yaml`\n- `operators/keystone/helm/keystone-operator/templates/serviceaccount.yaml`\n- `operators/keystone/helm/keystone-operator/templates/clusterrole.yaml`\n- `operators/keystone/helm/keystone-operator/templates/clusterrolebinding.yaml`\n- `operators/keystone/helm/keystone-operator/templates/webhook-configuration.yaml` (both MutatingWebhookConfiguration and ValidatingWebhookConfiguration)\n- `operators/keystone/helm/keystone-operator/templates/_helpers.tpl`\n- `deploy/flux-system/releases/keystone-operator.yaml` — FluxCD HelmRelease targeting `openstack` namespace\n- Update `deploy/flux-system/kustomization.yaml` to include the new HelmRelease resource\n\n**Excluded:**\n- CI/CD workflows for building/pushing operator image or Helm chart (S018 — separate feature)\n- Makefile `docker-build` and `helm-package` target implementations (separate feature per PLAN.md)\n- `values-prod.yaml` or environment-specific overrides (YAGNI)\n- `NOTES.txt` post-install template (YAGNI)\n- Helm chart tests (YAGNI for initial packaging)\n- HelmRepository source for c5c3-charts (`deploy/flux-system/sources/c5c3-charts.yaml` already exists)\n- Monitoring/alerting resources — PrometheusRule, ServiceMonitor (separate concern)\n- New namespace manifest (`openstack` already exists in `namespaces.yaml`)\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph build [\"Dockerfile - Multi-stage Build\"]\n        B1[\"golang:1.25 builder\"]\n        B2[\"CGO_ENABLED=0 go build\"]\n        B3[\"distroless/static:nonroot\"]\n        B1 --> B2 --> B3\n    end\n\n    subgraph helm [\"Helm Chart - keystone-operator\"]\n        CH[\"Chart.yaml\"]\n        VA[\"values.yaml\"]\n        CRD[\"crds/keystones.yaml\"]\n        subgraph templates [\"templates/\"]\n            DEP[\"deployment.yaml\"]\n            SVC[\"service.yaml\"]\n            SA[\"serviceaccount.yaml\"]\n            CR[\"clusterrole.yaml\"]\n            CRB[\"clusterrolebinding.yaml\"]\n            MWH[\"mutating-webhook-config\"]\n            VWH[\"validating-webhook-config\"]\n            HLP[\"_helpers.tpl\"]\n        end\n    end\n\n    subgraph flux [\"FluxCD GitOps\"]\n        SRC[\"HelmRepository: c5c3-charts<br/>oci://ghcr.io/c5c3/charts\"]\n        HR[\"HelmRelease: keystone-operator\"]\n        SRC --> HR\n    end\n\n    B3 -->|\"image referenced by\"| VA\n    VA -->|\"configures\"| templates\n    HR -->|\"deploys chart\"| helm\n```\n\n```mermaid\nsequenceDiagram\n    participant FC as FluxCD\n    participant HR as HelmRelease\n    participant H as Helm\n    participant K as Kubernetes\n    participant CM as cert-manager\n\n    FC->>HR: Reconcile keystone-operator\n    Note over HR: dependsOn: cert-manager,<br/>mariadb-operator, memcached-operator\n    HR->>H: Install/Upgrade chart\n    H->>K: Apply CRDs from crds/\n    H->>K: Create ServiceAccount\n    H->>K: Create ClusterRole + ClusterRoleBinding\n    H->>K: Create Deployment (2 replicas)\n    H->>K: Create Service (webhook 443 to 9443, metrics 8080)\n    H->>K: Create MutatingWebhookConfiguration\n    H->>K: Create ValidatingWebhookConfiguration\n    K->>CM: cert-manager.io/inject-ca-from annotation\n    CM-->>K: Inject CA bundle into webhook configs\n    K-->>H: Resources ready\n    H-->>HR: Release healthy\n    HR-->>FC: Ready=True\n```\n\n```mermaid\nflowchart LR\n    subgraph deps [\"HelmRelease Dependency Chain\"]\n        CM[\"cert-manager\"]\n        MDB[\"mariadb-operator\"]\n        MC[\"memcached-operator\"]\n        ES[\"external-secrets\"]\n        KS[\"keystone-operator\"]\n        CM --> MDB\n        CM --> MC\n        CM --> ES\n        MDB --> KS\n        MC --> KS\n        CM --> KS\n    end\n```\n\n## Key Components\n\n- **`operators/keystone/Dockerfile`**: Two-stage build. Builder stage uses `golang:1.25`, copies `go.work`, `go.sum`, and all three module directories (`internal/common/`, `operators/keystone/`, `operators/c5c3/`) for workspace-aware dependency resolution. Builds with `CGO_ENABLED=0 GOOS=linux go build -a -o manager ./operators/keystone/main.go`. Runtime stage copies binary to `gcr.io/distroless/static:nonroot`, runs as UID 65532:65532. Docker build context is workspace root (`/workspace`), not `operators/keystone/`.\n\n- **`Chart.yaml`**: Chart name `keystone-operator`, apiVersion `v2`, type `application`, version `0.1.0`, appVersion `0.1.0`.\n\n- **`values.yaml`**: Configurable image (repo/tag/pullPolicy), replicas (default 2), resource requests/limits, leaderElection toggle (default true), webhook enable/disable, metrics port (default 8080 — matches bootstrap default `--metrics-bind-address :8080`), serviceAccount name/create flag.\n\n- **`crds/keystone.openstack.c5c3.io_keystones.yaml`**: Copy of generated CRD from `config/crd/bases/`. Helm auto-installs CRDs from `crds/` directory before templates.\n\n- **`templates/deployment.yaml`**: Operator Deployment using `bootstrap.Run()` flags: `--leader-elect={{ .Values.leaderElection.enabled }}`, `--metrics-bind-address=:{{ .Values.metrics.port }}`, `--health-probe-bind-address=:8081`. Liveness/readiness probes on `:8081` (`/healthz`, `/readyz`). Security context: non-root, read-only rootfs, drop ALL capabilities, `allowPrivilegeEscalation: false`, `runAsNonRoot: true`. Resource limits from values. Webhook port 9443 exposed on container.\n\n- **`templates/service.yaml`**: ClusterIP service exposing webhook port (443→9443) and metrics port (8080).\n\n- **`templates/serviceaccount.yaml`**: Conditionally created SA via `{{ if .Values.serviceAccount.create }}`.\n\n- **`templates/clusterrole.yaml`**: RBAC rules derived from `+kubebuilder:rbac` markers in `keystone_controller.go`:\n  - `keystone.openstack.c5c3.io`: keystones, keystones/status, keystones/finalizers\n  - `apps`: deployments\n  - `\"\"` (core): services, configmaps, secrets, serviceaccounts, events\n  - `batch`: jobs, cronjobs\n  - `k8s.mariadb.com`: databases, users, grants\n  - `external-secrets.io`: externalsecrets, pushsecrets\n  - `rbac.authorization.k8s.io`: roles, rolebindings\n\n- **`templates/clusterrolebinding.yaml`**: Binds ClusterRole to the operator's ServiceAccount in the `openstack` namespace.\n\n- **`templates/webhook-configuration.yaml`**: Both `MutatingWebhookConfiguration` (defaulting, path `/mutate-keystone-openstack-c5c3-io-v1alpha1-keystone`, operations: CREATE+UPDATE, name: `mkeystone.kb.io`) and `ValidatingWebhookConfiguration` (validation, path `/validate-keystone-openstack-c5c3-io-v1alpha1-keystone`, operations: CREATE+UPDATE+DELETE, name: `vkeystone.kb.io`). Both with `failurePolicy: Fail`, `sideEffects: None`, and `cert-manager.io/inject-ca-from` annotation for automatic CA bundle injection.\n\n- **`templates/_helpers.tpl`**: Standard helpers — `fullname`, `name`, `chart`, `labels`, `selectorLabels`, `serviceAccountName`.\n\n- **`deploy/flux-system/releases/keystone-operator.yaml`**: HelmRelease (apiVersion `helm.toolkit.fluxcd.io/v2`) targeting `openstack` namespace. References `c5c3-charts` OCI HelmRepository (namespace `flux-system`). `dependsOn: [cert-manager, mariadb-operator, memcached-operator]` with correct namespace references. `install.crds: CreateReplace`, `upgrade.crds: CreateReplace`, `remediation.retries: 3`. Version range `>=0.1.0 <1.0.0`. Interval `30m`. Values override: `replicas: 2`, `leaderElection.enabled: true`.\n\n- **`deploy/flux-system/kustomization.yaml`**: Add `releases/keystone-operator.yaml` to the resources list (alongside existing releases).",
  "stories": [],
  "requirements": [],
  "tasks": [],
  "test_specifications": [],
  "affected_files": [],
  "similar_patterns": [],
  "review_criteria": [],
  "implementation_notes": "",
  "status_history": {
    "draft": {
      "github_account": "berendt",
      "timestamp": "2026-02-22T20:42:36.268806"
    },
    "elaborated": {
      "github_account": "berendt",
      "timestamp": "2026-03-22T21:04:29.498496"
    }
  },
  "origin": "plan",
  "devils_advocate_report": {
    "verdict": "RECONSIDER",
    "summary": "Failed to parse challenge report: no XML found in output",
    "challenges": [],
    "risk_flags": [],
    "alternatives": []
  }
}