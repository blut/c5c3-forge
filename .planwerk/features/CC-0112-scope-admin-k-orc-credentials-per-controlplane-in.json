{
  "feature_id": "CC-0112",
  "title": "Scope admin/K-ORC credentials per ControlPlane in OpenBao",
  "slug": "scope-admin-k-orc-credentials-per-controlplane-in",
  "status": "prepared",
  "phase": null,
  "summary": "",
  "description": "# GitHub Issue #414: feat(c5c3-operator): make the admin/K-ORC credential path multi-instance safe (per-ControlPlane OpenBao scoping)\n\n**Description:**\n\n`c5c3-operator` already projects N `ControlPlane` CRs into N namespaces in a way that *creates* them all without name collisions. The Helm chart watches cluster-wide by default (`operators/c5c3/helm/c5c3-operator/values.yaml:24` — `rbac.namespaceScoped: false`) and `bootstrap.Run` leaves `cache.Options.DefaultNamespaces` unset (`internal/common/bootstrap/manager.go:68–78`) so every namespace's `ControlPlane` is reconciled. `childNamespace(cp) = cp.Namespace` (`operators/c5c3/internal/controller/reconcile_infrastructure.go:43–45`) projects every child (`MariaDB`, `Memcached`, `Keystone`, K-ORC `ApplicationCredential`/`User`/`Domain`/`Service`/`Endpoint`, the operator-owned admin Secret and the backup `PushSecret`) into the owning ControlPlane's namespace, and every name is `cp.Name`-prefixed (`adminAppCredentialName`, `adminPasswordCloudSecretName`, `adminAppCredentialSecretName`, `keystoneServiceName`, `keystoneEndpointName`, `adminDomainRef` — all in `reconcile_korc.go:97–113`, `1219–1225`, `683–685`).\n\nThe collision is purely on the **remote** identifiers: OpenBao is a single cluster-global backend reached via one `ClusterSecretStore` (`openbao-cluster-store`, `reconcile_korc.go:42`), and four objects on that backend are *not* parameterised per CR.\n\n### Concrete boundaries (already exists ↔ this issue adds)\n\n1. **Admin AC OpenBao path — hardcoded across consumers.**\n   - *Exists:* `const adminAppCredentialRemoteKey = \"openstack/keystone/admin/app-credential\"` (`operators/c5c3/internal/controller/reconcile_korc.go:58`), used by `adminAppCredentialPushSecret` at `reconcile_korc.go:1086`. The matching consumer reads the same key in *both* the control-plane namespace and `orc-system`: `deploy/eso/externalsecrets/k-orc-clouds-yaml.yaml:49,69`. The OpenBao ACL grants this literal leaf and only this leaf — `deploy/openbao/policies/push-app-credentials.hcl:34,38`. The bootstrap seed writes this exact path in `deploy/openbao/bootstrap/write-bootstrap-secrets.sh:131,214`.\n   - *Issue adds:* a builder `adminAppCredentialRemoteKeyFor(cp *ControlPlane) string` returning `openstack/keystone/<cp.Namespace>/<cp.Name>/admin/app-credential`. Replace the constant at every write call site (the c5c3 PushSecret builder) and at every read consumer (the operator-owned `k-orc-clouds-yaml` ExternalSecret(s) — see boundary 7). Broaden the policy from one literal leaf to one `+`/`+` two-segment template that *still* excludes the read-only `db` and `+/fernet-keys`/`+/credential-keys` shapes already granted by `push-keystone-keys.hcl`.\n\n2. **Admin-password OpenBao path — hardcoded in `keystone-operator`.**\n   - *Exists:* `RemoteKey: \"bootstrap/keystone-admin\"` (`operators/keystone/internal/controller/reconcile_passwordrotation.go:603`), with the same single-CR assumption documented on the type (`operators/keystone/api/v1alpha1/keystone_types.go:403–407` — *\"Model B assumes a single Model-B-enabled Keystone CR per cluster\"*) and reiterated in the policy header (`deploy/openbao/policies/push-keystone-admin.hcl:18–29`). The consumer `ExternalSecret` reads this same flat key (`deploy/eso/externalsecrets/keystone-admin.yaml:27`). The bootstrap seed writes it at `write-bootstrap-secrets.sh:192`.\n   - *Issue adds:* a builder that returns `bootstrap/<keystone.Namespace>/<keystone.Name>/admin` (or `…/<keystone.Name>/admin`). Repoint the consumer to the matching per-CR key and migrate the bootstrap seed (boundaries 5, 6, 7).\n\n3. **K-ORC `User` import name not CR-scoped.**\n   - *Exists:* `adminUserRef(cp)` returns `cp.Spec.KORC.AdminCredential.CloudCredentialsRef.CloudName` (`reconcile_korc.go:666–671`), default `\"admin\"`. The sibling helper `adminDomainRef` *does* return `cp.Name + \"-domain-default\"` (`reconcile_korc.go:683–685`), so the inconsistency is local to the `User` import. Two `ControlPlane` CRs in the *same* namespace would `CreateOrUpdate` the same `User` object via `ensureKORCAdminImports` at `reconcile_korc.go:708–723`, flapping `ownerReferences` and spec.\n   - *Issue adds:* prefix the import CR's `metadata.name` with `cp.Name` (e.g. `cp.Name + \"-user-admin\"`), mirroring `adminDomainRef`. Keep `User.Spec.Import.Filter.Name = openstack name \"admin\"` so the underlying OpenStack user resolved by K-ORC is unchanged — only the Kubernetes object name changes.\n\n4. **Per-CR fernet/credential paths still collide across namespaces on identical CR names.**\n   - *Exists:* CC-0093 already moved fernet/credential PushSecrets to `openstack/keystone/{keystone.Name}/{fernet,credential}-keys` (`operators/keystone/internal/controller/reconcile_fernet.go:455`, `reconcile_credential.go:459`) and the policy to the `+` single-segment template `kv-v2/data/openstack/keystone/+/{fernet,credential}-keys` (`deploy/openbao/policies/push-keystone-keys.hcl:73–85`). Two `Keystone` CRs with identical names in different namespaces still resolve to the same OpenBao leaf.\n   - *Issue adds:* a per-CR-name **and** per-namespace segment. Decide explicitly: either add a `{keystone.Namespace}` segment (`openstack/keystone/<ns>/<name>/{fernet,credential}-keys`) — robust to name collisions, requires broadening the policy template to `kv-v2/data/openstack/keystone/+/+/{fernet,credential}-keys` — or enforce CR-name uniqueness via the admission guard from boundary 8 so the existing `+` template stays valid. Pick one and record it as a Decision in the source.\n\n5. **Bootstrap seed is single-tenant.**\n   - *Exists:* `seed_korc_bootstrap_clouds_yaml()` at `deploy/openbao/bootstrap/write-bootstrap-secrets.sh:130–182` writes exactly one `kv-v2/openstack/keystone/admin/app-credential`. `main()` (`write-bootstrap-secrets.sh:187–217`) seeds exactly one `kv-v2/bootstrap/keystone-admin`. Both paths are then `mark_eso_managed` so ESO can adopt them.\n   - *Issue adds:* either (a) make the bootstrap script accept a list of ControlPlane identities (e.g. `KORC_CONTROLPLANES=\"openstack/controlplane-keystone openstack/controlplane-staging\"`) and seed one per-CR path per identity, or (b) move the seed responsibility into the c5c3-operator itself so the first reconcile pass writes the per-CR seed. Option (b) is the natural endpoint when combined with the operator-owned `k-orc-clouds-yaml` ExternalSecret called out in #412 / boundary 7; (a) is the lower-risk interim option that keeps the cluster-bootstrap path unchanged. Pick one explicitly.\n\n6. **No admission guard on `ControlPlane` count per namespace.**\n   - *Exists:* `ControlPlaneWebhook.validate()` at `operators/c5c3/api/v1alpha1/controlplane_webhook.go:123–193` enforces release-pattern, database XOR, cache XOR, password-secret-ref required, rotation-interval admissibility — but does *not* count existing CRs in the namespace. The only signal today is `CredentialRotationReconciler.resolveControlPlane` returning Reason `\"AmbiguousControlPlane\"` (`reconcile_credentialrotation.go:202–230`, documented at `docs/reference/c5c3/controlplane-reconciler.md:589–610`) *after* the clobber has already happened.\n   - *Issue adds:* a Decision and the corresponding implementation: either (a) enforce one `ControlPlane` per namespace in `ControlPlaneWebhook.ValidateCreate` (using the injected `Client` to list `ControlPlanes` in the request namespace), and matching the CredentialRotation contract by then dropping the `AmbiguousControlPlane` branch; or (b) make the cluster genuinely multi-instance (boundaries 1–5 already on this issue) and replace the `resolveControlPlane` ambiguity check with a `ControlPlaneRef` field on `CredentialRotation`. Note that path (b) is a CRD schema addition. The issue's title and \"multi-instance safe\" framing implies (b); (a) is the minimal-change option if multi-instance per namespace is rejected.\n\n7. **K-ORC `clouds.yaml` ExternalSecret is statically named and not per-CR.**\n   - *Exists:* `deploy/eso/externalsecrets/k-orc-clouds-yaml.yaml` defines two static `ExternalSecret`s, both named `k-orc-clouds-yaml`, one in `openstack` and one in `orc-system`, both reading the fixed key `openstack/keystone/admin/app-credential`. `reconcileAdminCredential` gates on `secrets.WaitForExternalSecret(childNamespace(cp)/CloudCredentialsRef.SecretName)` (`reconcile_korc.go:880–902`) — `CloudCredentialsRef.SecretName` defaults to `\"k-orc-clouds-yaml\"` via the CRD default and the webhook (`controlplane_webhook.go:32–33,80–83`).\n   - *Issue adds:* the operator owns and templates this ExternalSecret per `ControlPlane` (one per CR in `childNamespace(cp)`, name = `CloudCredentialsRef.SecretName`, key = the per-CR OpenBao path from boundary 1). The `orc-system` global copy is either removed (the AdminCredentialReady gate is already against the CP-namespace ExternalSecret) or also operator-owned and made per-CR. This is the work #412 covers; #414 cannot move the OpenBao path without it because the static `key:` would otherwise drift from the writer.\n\n8. **Existing single-tenant data needs a migration plan.**\n   - *Exists:* clusters that already ran the operator hold one `openstack/keystone/admin/app-credential` and one `bootstrap/keystone-admin` object. Pre-existing fernet/credential PushSecrets at `openstack/keystone/{name}/{fernet,credential}-keys` are already per-CR (CC-0093).\n   - *Issue adds:* a written one-time migration step (operator runbook): for each ControlPlane, copy the legacy values into the new per-CR paths before bringing the new operator up, then orphan the legacy leaves (the operator does not write them). Boundary 4's decision also implies, in the namespace-segment variant, a one-time copy of fernet/credential leaves into the namespace-prefixed shape.\n\n9. **What this issue does NOT touch.**\n   - The K-ORC `Service`/`Endpoint`/`Domain`/`ApplicationCredential` CRs themselves: they already embed `cp.Name` and live in `childNamespace(cp)`. The admin password Secret (`PasswordSecretRef`) and the cleartext-cloudsyaml `<cp.Name>-admin-password-cloud` Secret (`reconcile_korc.go:101–105,216–234`): they are already per-CR. The K-ORC `cp.Spec.KORC.AdminCredential.CloudCredentialsRef.SecretName` value: defaulted today, kept defaulted but documented as the per-CR ExternalSecret name once boundary 7 lands.\n\n**Motivation:**\n\nThe current behaviour already provisions N `ControlPlane` CRs in N namespaces without any in-cluster name clash — the operator's own owner-reference and `cp.Name` prefixing discipline is sound. What breaks at *operate* time is purely the four OpenBao identifiers in boundaries 1, 2, 3, and 4: the last writer wins, K-ORC then authenticates with the wrong namespace's credential, and `CredentialRotation` only flags the situation under reason `AmbiguousControlPlane` *after* the clobber. The shape of the bug (the operator advertises namespace-scoped projection but the backing store is cluster-flat) is the same class of issue as CC-0093, which already proved out the per-CR path layout, the `+`-glob policy template, and the migration-note discipline for the fernet/credential leaves; this issue extends the same pattern to the admin AC and admin password.\n\nClosing #414 unblocks two follow-on goals: (a) running a staging and a production `ControlPlane` side-by-side in two namespaces on a single cluster — the documented test setup of `tests/e2e/c5c3/full-controlplane-keystone/` only has one — and (b) the namespace-isolation story the CC-0110 reconciler-reference doc already frames around C1 (`docs/reference/c5c3/controlplane-reconciler.md:507,519`). #412 (operator-owned `k-orc-clouds-yaml` ExternalSecret) is the tightest coupling: that issue cannot land without this one (the read key has to match the per-CR write), and this issue cannot fully land without that one (a per-CR write path with no matching per-CR reader is dead work). They should ship together, with the OpenBao paths and User name landing here and the ExternalSecret templating landing under #412.\n\nThe work also pays back debt elsewhere: every \"single Model-B-enabled Keystone CR per cluster\" sentence in the source (`reconcile_passwordrotation.go:571–574`, `keystone_types.go:403–407`) and policy headers (`push-keystone-admin.hcl:18–29`, `push-app-credentials.hcl:14–22`) becomes deletable.\n\n**Affected Areas:**\n\n- operators/c5c3/internal/controller/reconcile_korc.go (replace the adminAppCredentialRemoteKey constant at line 58 with a builder taking *c5c3v1alpha1.ControlPlane; thread it through adminAppCredentialPushSecret at line 1066–1092; rename adminUserRef at line 666–671 to return cp.Name + \"-user-admin\" while keeping the OpenStack-name filter inside ensureKORCAdminImports unchanged; reconcileAdminCredential at 854–1045 keeps reading the per-CR ExternalSecret name from CloudCredentialsRef.SecretName)\n- operators/c5c3/internal/controller/reconcile_korc_test.go (update every assertion pinning the literal openstack/keystone/admin/app-credential at lines 81, 220, 373, 389, 696, 720, 878, 1012, 1083 and expectations on adminUserRef results to the per-CR builder)\n- operators/c5c3/internal/controller/integration_test.go (adjust envtest scenarios to bring up two ControlPlanes; today only one is exercised)\n- operators/c5c3/api/v1alpha1/controlplane_webhook.go (Decision-dependent: either add the one-per-namespace admission check using the injected Client at lines 47–49, or leave the webhook untouched per the boundary 6 Decision (b) path)\n- operators/c5c3/api/v1alpha1/credentialrotation_types.go (Decision-dependent: if multi-instance per namespace is chosen, add a ControlPlaneRef *corev1.LocalObjectReference to CredentialRotationSpec and document the AmbiguousControlPlane deprecation)\n- operators/c5c3/internal/controller/reconcile_credentialrotation.go (keep the resolveControlPlane indirection but switch the body to either \"exactly one allowed by admission, never see >1\" or \"use the explicit ControlPlaneRef\")\n- operators/c5c3/api/v1alpha1/controlplane_types.go, operators/c5c3/config/crd/bases/c5c3.io_controlplanes.yaml, operators/c5c3/helm/c5c3-operator/crds/c5c3.io_controlplanes.yaml (regenerate via make manifests/make sync-crds if the CRD surface changes per boundary 6 path b)\n- operators/keystone/internal/controller/reconcile_passwordrotation.go (replace the hardcoded RemoteKey: \"bootstrap/keystone-admin\" at line 603 with a per-CR builder such as fmt.Sprintf(\"bootstrap/%s/%s/admin\", keystone.Namespace, keystone.Name); update the type comment on PasswordRotationSpec at keystone_types.go:403–407 to drop the single-CR assumption)\n- operators/keystone/internal/controller/reconcile_passwordrotation_test.go (update adminPasswordPushSecret test assertions on RemoteKey)\n- operators/keystone/internal/controller/reconcile_fernet.go and reconcile_credential.go (Decision-dependent on boundary 4: if the namespace-segment variant is chosen, broaden the RemoteKey builder to include keystone.Namespace)\n- operators/keystone/internal/controller/reconcile_fernet_test.go, reconcile_credential_test.go, integration_test.go (matching test updates per the boundary 4 Decision)\n- deploy/eso/externalsecrets/k-orc-clouds-yaml.yaml (eliminated or templated by #412; the c5c3 PushSecret writer in this issue and the ExternalSecret reader in #412 must agree on the per-CR key:)\n- deploy/eso/externalsecrets/keystone-admin.yaml (eliminated or templated to match the per-CR bootstrap/<ns>/<name>/admin key; if left static, this issue is incomplete)\n- deploy/openbao/policies/push-app-credentials.hcl (replace lines 34–40 with a +/+ two-segment template covering kv-v2/{data,metadata}/openstack/keystone/+/+/admin/app-credential; keep the data-only-vs-metadata rationale block)\n- deploy/openbao/policies/push-keystone-admin.hcl (replace the two literal leaves at lines 54–60 with kv-v2/{data,metadata}/bootstrap/+/+/admin; rewrite the policy header at lines 18–29 to drop the single-CR justification and document the new two-segment-glob semantics)\n- deploy/openbao/policies/push-keystone-keys.hcl (boundary 4 path-dependent: if the namespace-segment variant is chosen, update from kv-v2/data/openstack/keystone/+/{fernet,credential}-keys to kv-v2/data/openstack/keystone/+/+/{fernet,credential}-keys and update the comment on /db exclusion)\n- deploy/openbao/policies/eso-management.hcl (verify the trailing wildcard kv-v2/data/openstack/keystone/* at line 34 still covers the new shape for the read side; widen if needed)\n- deploy/openbao/bootstrap/write-bootstrap-secrets.sh (boundary 5 path-dependent: if the bootstrap remains responsible for seeding, take a list of <namespace>/<name> ControlPlane identities as env var or CLI and loop the seed + mark_eso_managed calls; update seed_korc_bootstrap_clouds_yaml() at lines 130–182 and main() at lines 187–217 accordingly)\n- deploy/kind/controlplane/controlplane.yaml (confirm the example matches the new naming convention; surface that the kind dev cluster still ships one ControlPlane by default)\n- tests/e2e/c5c3/full-controlplane-keystone/chainsaw-test.yaml (update sub-asserts if the literal openstack/keystone/admin/app-credential path appears, since assertions reference it indirectly through the k-orc-clouds-yaml Secret name)\n- tests/e2e/c5c3/multi-controlplane/ (new sibling test directory exercising two ControlPlane CRs in tenant-a and tenant-b namespaces in parallel; covers both CRs reaching Ready=True simultaneously, killing AC-A not disturbing CP-B, and kubectl describe controlplane showing distinct status.adminApplicationCredential.id)\n- docs/reference/c5c3/controlplane-reconciler.md (update lines 507, 519, 529, 641 to reference the per-CR remote key; replace the AmbiguousControlPlane row at line 609 per the boundary 6 decision; add a \"Multi-instance\" section explaining the per-CR path layout, the one-per-namespace contract or its removal, and the migration note)\n- docs/reference/c5c3/controlplane-crd.md (update the CloudCredentialsRef.SecretName default doc if anything changes; record the per-CR layout under a Status / Multi-instance section)\n- docs/reference/keystone/keystone-reconciler.md (update the admin password rotation PushSecret table at lines 2728–2744 and remove the \"single-CR assumption, boundary 8\" caveat)\n- docs/reference/infrastructure/infrastructure-manifests.md (update lines 687, 699, 704–705 to describe the per-CR template)\n- architecture/docs/09-implementation/* (git submodule per .gitmodules; record the architecture-level change as a separate upstream PR — do not modify from this worktree, matching the discipline used in CC-0093 REQ-008)\n\n**Acceptance Criteria:**\n\n- [ ] adminAppCredentialRemoteKey is no longer a package-level constant; in its place a builder adminAppCredentialRemoteKeyFor(cp) returns a path that contains both cp.Namespace and cp.Name as path segments, and every write (the c5c3 PushSecret builder) plus every documented read (the per-CR ExternalSecret materialised by #412) targets it.\n- [ ] The keystone-operator's adminPasswordPushSecret RemoteKey is computed per-CR (contains both keystone.Namespace and keystone.Name segments) and the matching ExternalSecret (deploy/eso/externalsecrets/keystone-admin.yaml or its replacement) reads the same key.\n- [ ] ensureKORCAdminImports creates a K-ORC User whose Kubernetes metadata.name is cp.Name + \"-user-admin\" (or equivalent prefix) so two ControlPlanes in the same namespace produce two distinct User objects; the inner Spec.Import.Filter.Name still resolves to OpenStack user admin so the K-ORC import behaviour is unchanged.\n- [ ] Boundary 4 is resolved by a documented Decision: either (a) the fernet/credential keys' RemoteKey includes a keystone.Namespace segment and the policy template adopts +/+/{fernet,credential}-keys; or (b) the admission guard from boundary 6 enforces CR-name uniqueness cluster-wide for Keystone CRs. The Decision is recorded as a comment block at the call site (reconcile_fernet.go:455, reconcile_credential.go:459).\n- [ ] Boundary 6 is resolved by a documented Decision and matching implementation: either (a) ControlPlaneWebhook.ValidateCreate rejects a ControlPlane when another exists in the same namespace, AND CredentialRotationReconciler keeps AmbiguousControlPlane purely as a defense-in-depth fallback; or (b) CredentialRotationSpec carries an explicit ControlPlaneRef and the AmbiguousControlPlane branch is removed from resolveControlPlane and from the reconciler docs.\n- [ ] deploy/openbao/policies/push-app-credentials.hcl and deploy/openbao/policies/push-keystone-admin.hcl grant create/update/read/delete on data-and-metadata paths matching the per-CR templates, still deny anything outside those templates, and carry rationale comments referencing this issue's ID.\n- [ ] deploy/openbao/bootstrap/write-bootstrap-secrets.sh either accepts and iterates a list of ControlPlane identities (option a on boundary 5) or has its seed responsibility removed in favour of operator-owned seeding (option b). The script does not silently keep seeding the legacy flat paths on a multi-CR cluster.\n- [ ] An E2E suite under tests/e2e/c5c3/multi-controlplane/ (or equivalent) brings up two ControlPlane CRs in two namespaces in the same kind cluster, asserts both reach Ready=True simultaneously with distinct status.adminApplicationCredential.id, rotates the admin AC of one CR via CredentialRotation, and asserts the other CR's AC is unaffected (status.adminApplicationCredential.lastRotation unchanged, AC Available=True throughout).\n- [ ] Existing tests/e2e/c5c3/full-controlplane-keystone/ chainsaw test still passes against the per-CR path layout — i.e. the new write/read agreement does not regress the single-CR happy path.\n- [ ] docs/reference/c5c3/controlplane-reconciler.md, controlplane-crd.md, docs/reference/keystone/keystone-reconciler.md, and docs/reference/infrastructure/infrastructure-manifests.md describe the per-CR path layout and the multi-instance contract; the \"one Model-B-enabled Keystone CR per cluster\" and \"single admin AC per cluster\" wording in source code comments and the policy file headers is removed.\n- [ ] A MIGRATION.md (or a \"Migration\" section in the controlplane-reconciler doc) describes the one-time copy of kv-v2/openstack/keystone/admin/app-credential and kv-v2/bootstrap/keystone-admin (and, on path-(a) of boundary 4, the fernet/credential leaves) into the per-CR layout, including the bao kv put/bao kv list commands and the order with respect to the operator upgrade.\n\n**Non-Goals:**\n\n- Implementing the scheduled rotation loop on CredentialRotation (intervalDays, preRotationDays, gracePeriodDays) — it remains \"read-but-ignored\" per reconcile_credentialrotation.go:96–110 and credentialrotation_types.go:67–95, out of scope for the multi-instance safety work.\n- Adding multi-cluster (Cluster.multitenant style) tenancy — the change is per-namespace within a single cluster, not cross-cluster.\n- Re-architecting the cluster-global openbao-cluster-store — the ClusterSecretStore stays cluster-scoped because it has to reach OpenBao from every namespace; only the paths underneath become per-CR.\n- Changing the K-ORC CloudCredentialsRef.SecretName default (controlplane_webhook.go:33) or the cloudName default — they continue to default to \"k-orc-clouds-yaml\" / \"admin\" so a single-CR cluster's manifests are untouched.\n- Modifying the architecture/ submodule from this worktree — architecture-doc updates ship as a separate upstream PR, matching CC-0093 REQ-008 discipline.\n- Backfilling per-CR data on the live OpenBao backend automatically — the one-time migration is operator-driven via bao kv put per the migration note; the operator does not read or copy the legacy paths.\n\n**References:**\n\n- operators/c5c3/internal/controller/reconcile_korc.go (admin AC path constant, push-secret builder, User import name)\n- operators/c5c3/internal/controller/reconcile_credentialrotation.go (one-per-namespace contract and AmbiguousControlPlane)\n- operators/c5c3/internal/controller/controlplane_controller.go (sub-reconciler ordering, cp.Namespace projection model)\n- operators/c5c3/internal/controller/reconcile_infrastructure.go:43–45 (childNamespace(cp) = cp.Namespace)\n- operators/c5c3/api/v1alpha1/controlplane_webhook.go (defaulting + validation; injected Client)\n- operators/c5c3/api/v1alpha1/controlplane_types.go, credentialrotation_types.go\n- operators/keystone/internal/controller/reconcile_passwordrotation.go:571–610 (bootstrap/keystone-admin hardcoded)\n- operators/keystone/internal/controller/reconcile_fernet.go:455, reconcile_credential.go:459 (existing per-CR pattern from CC-0093)\n- deploy/openbao/bootstrap/write-bootstrap-secrets.sh:130–182,187–217\n- deploy/eso/externalsecrets/k-orc-clouds-yaml.yaml, keystone-admin.yaml, keystone-db.yaml\n- deploy/openbao/policies/push-app-credentials.hcl, push-keystone-admin.hcl, push-keystone-keys.hcl, eso-management.hcl\n- internal/common/bootstrap/manager.go:63–78,113–134 (DefaultNamespaces cache scoping)\n- tests/e2e/c5c3/full-controlplane-keystone/ (single-CR happy path)\n- tests/e2e/keystone/concurrent-cr-conflicts/ (existing pattern for two CRs in one namespace; mirror for two namespaces)\n- docs/reference/c5c3/controlplane-reconciler.md (sections reconcileAdminCredential, CredentialRotation reconciler, K-ORC admin credential chain)\n- docs/reference/keystone/keystone-reconciler.md:2720–2766 (Model B PushSecret table)\n- docs/reference/infrastructure/infrastructure-manifests.md:687–705 (push-app-credentials policy doc)\n- Related issue CC-0093 (per-CR RemoteKey for fernet/credential keys; same template-glob pattern this issue extends) — .planwerk/completed/CC-0093-parameterize-openbao-remotekey-per-keystone-cr.json\n- Related issue CC-0110 (introduces ControlPlane, reconcileKORC, reconcileAdminCredential) — .planwerk/completed/CC-0110-implement-c5c3-operator-controlplane-for-keystone.json\n- Related issue CC-0109 (Model B admin-password rotation, owner of bootstrap/keystone-admin writer) — .planwerk/completed/CC-0109-schedule-keystone-admin-password-rotation-via.json\n- Related issue #412 (operator-owned k-orc-clouds-yaml ExternalSecret) — lands together with this for the k-orc-clouds-yaml half\n- Review patterns drawn on: Hardcoded sub-resource names prevent multi-instance support, Centralize duplicated status condition strings, Apply security mitigations consistently across all files, Cross-reference access policies against all consumers, Enforce least-privilege on wildcard access paths, Verify migration notes cover brownfield upgrade paths\n\n---\n\n_Elaborated by [planwerk-review](https://github.com/planwerk/planwerk-review) with Claude Code_\n\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/414",
  "stories": [
    {
      "title": "Operator engineer runs two ControlPlanes in two namespaces without OpenBao collisions",
      "role": "operator engineer",
      "want": "each ControlPlane's minted admin ApplicationCredential to be mirrored to an OpenBao KV-v2 path scoped by both the ControlPlane namespace and name",
      "so_that": "a staging and a production ControlPlane in separate namespaces on one cluster never overwrite each other's admin credential and K-ORC always authenticates with the correct tenant's credential",
      "criteria": [
        "adminAppCredentialPushSecret(cp) returns RemoteKey == openstack/keystone/{cp.Namespace}/{cp.Name}/admin/app-credential",
        "adminAppCredentialRemoteKey is no longer a package-level constant; it is a builder taking *c5c3v1alpha1.ControlPlane",
        "Two ControlPlanes (openstack/cp-a and tenant-b/cp-b) produce two distinct RemoteKey values that both literally contain their Namespace and Name segments",
        "The minted admin AC Secret name, PushSecret name, password-cloud Secret name and Domain name remain cp.Name-prefixed exactly as before (no regression)"
      ]
    },
    {
      "title": "Keystone operator scopes the admin-password backup path per Keystone CR",
      "role": "keystone platform engineer",
      "want": "adminPasswordPushSecret to write to bootstrap/{keystone.Namespace}/{keystone.Name}/admin instead of the flat bootstrap/keystone-admin leaf",
      "so_that": "two Model-B Keystone CRs (even with the same name in different namespaces) never clobber each other's rotated admin password in OpenBao",
      "criteria": [
        "adminPasswordPushSecret(keystone).Spec.Data[0].Match.RemoteRef.RemoteKey == bootstrap/{keystone.Namespace}/{keystone.Name}/admin",
        "The 'Model B assumes a single Model-B-enabled Keystone CR per cluster' sentence is removed from keystone_types.go and the builder godoc",
        "The matching deploy/eso/externalsecrets/keystone-admin.yaml ExternalSecret reads the same per-CR key for the default deployment identity",
        "DeletionPolicy stays None and Property stays adminPasswordSecretKey (no behavioural regression to the rotation flow)"
      ]
    },
    {
      "title": "Operator engineer gets distinct K-ORC User objects per ControlPlane",
      "role": "operator engineer",
      "want": "the imported K-ORC admin User CR's Kubernetes metadata.name to be cp.Name-prefixed like the sibling admin Domain",
      "so_that": "two ControlPlanes in the same namespace produce two distinct User objects instead of fighting over ownerReferences on one shared 'admin' User",
      "criteria": [
        "adminUserRef(cp) returns cp.Name + \"-user-admin\" (mirrors adminDomainRef's cp.Name + \"-domain-default\")",
        "ensureKORCAdminImports creates User{metadata.name: cp.Name + \"-user-admin\"} owned by the ControlPlane",
        "User.Spec.Import.Filter.Name still resolves to OpenStack username 'admin' (korcAdminUsername) so K-ORC import behaviour is unchanged",
        "The ApplicationCredential.Spec.Resource.UserRef points at the new cp.Name-prefixed User name"
      ]
    },
    {
      "title": "Security reviewer confirms OpenBao ACLs stay least-privilege under the per-CR templates",
      "role": "security reviewer",
      "want": "the push-app-credentials, push-keystone-admin and push-keystone-keys policies to grant only the new per-CR glob shapes and still deny the db / cross-leaf shapes",
      "so_that": "broadening the literal leaves to +/+ templates does not let a compromised ESO token overwrite unrelated bootstrap or database secrets",
      "criteria": [
        "push-app-credentials.hcl grants create/update/read on kv-v2/{data,metadata}/openstack/keystone/+/+/admin/app-credential and nothing wider",
        "push-keystone-admin.hcl grants create/update/read/delete on kv-v2/{data,metadata}/bootstrap/+/+/admin and no trailing-* over the bootstrap subtree",
        "push-keystone-keys.hcl uses kv-v2/{data,metadata}/openstack/keystone/+/+/{fernet,credential}-keys and the /db lock-out rationale still holds",
        "Every policy header cites CC-0112 and the upstream +-glob single-segment semantics; the single-CR justification blocks are removed"
      ]
    },
    {
      "title": "Cluster admin is prevented from creating a second ControlPlane in one namespace",
      "role": "cluster administrator",
      "want": "ControlPlaneWebhook.ValidateCreate to reject a ControlPlane when another already exists in the same namespace",
      "so_that": "the one-ControlPlane-per-namespace contract that CredentialRotation.resolveControlPlane already assumes is enforced at admission time rather than discovered as AmbiguousControlPlane after a clobber",
      "criteria": [
        "Creating a second ControlPlane in a namespace that already has one is rejected with a clear field error naming the existing ControlPlane",
        "Creating the first ControlPlane in a namespace, or updating an existing one, is still admitted",
        "CredentialRotationReconciler.resolveControlPlane keeps the AmbiguousControlPlane branch as a defense-in-depth fallback (it must never fire when the webhook is active)",
        "No new CRD field or CRD schema version is introduced (controlplane CRD bytes unchanged)"
      ]
    },
    {
      "title": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "role": "site reliability engineer",
      "want": "a written one-time migration runbook covering the admin AC, admin password and fernet/credential leaves",
      "so_that": "an in-place operator upgrade does not lock K-ORC or Keystone out by leaving the readers pointed at paths the new writers no longer populate",
      "criteria": [
        "A Migration section gives exact bao kv get (legacy) -> bao kv put (per-CR) commands for openstack/keystone/admin/app-credential and bootstrap/keystone-admin",
        "It states the order: copy legacy -> new path, re-apply the three ACLs, then upgrade the operators",
        "It documents the boundary-4 namespace-segment copy of the fernet/credential leaves and the bao kv metadata delete purge of the orphaned legacy leaves",
        "It mirrors the CC-0093 'orphaned but harmless' wording and is reachable from controlplane-reconciler.md and keystone-reconciler.md"
      ]
    },
    {
      "title": "E2E suite proves two ControlPlanes are isolated end-to-end",
      "role": "QA / release engineer",
      "want": "a chainsaw suite that brings up two ControlPlanes in two namespaces and asserts credential isolation across a rotation",
      "so_that": "the multi-instance guarantee is regression-protected in CI, not only at the unit/builder level",
      "criteria": [
        "tests/e2e/c5c3/multi-controlplane/ brings up ControlPlanes in tenant-a and tenant-b and both reach Ready=True with distinct status.adminApplicationCredential.id",
        "Rotating the admin AC of tenant-a (via CredentialRotation) leaves tenant-b's status.adminApplicationCredential.lastRotation unchanged and its AC Available=True throughout",
        "The two OpenBao paths openstack/keystone/tenant-a/<cp>/admin/app-credential and openstack/keystone/tenant-b/<cp>/admin/app-credential both exist and differ",
        "The existing tests/e2e/c5c3/full-controlplane-keystone/ single-CR happy path still passes against the per-CR layout"
      ]
    },
    {
      "title": "Operator and reference docs describe the per-CR layout and drop the single-CR caveats",
      "role": "documentation reader / new joiner",
      "want": "controlplane-reconciler.md, controlplane-crd.md, keystone-reconciler.md and infrastructure-manifests.md to show the per-CR paths and a Multi-instance section",
      "so_that": "the docs match the code and no longer claim 'one Model-B-enabled Keystone CR per cluster' or 'single admin AC per cluster'",
      "criteria": [
        "Every documented occurrence of openstack/keystone/admin/app-credential and bootstrap/keystone-admin shows the per-CR template instead",
        "The AmbiguousControlPlane row is updated to describe the admission guard + defense-in-depth fallback",
        "A new Multi-instance section explains the per-CR path layout and the one-ControlPlane-per-namespace contract",
        "The architecture/ submodule is NOT edited from this worktree; the architecture-level change is flagged as a separate upstream PR"
      ]
    }
  ],
  "requirements": [
    {
      "id": "REQ-001",
      "description": "The c5c3-operator SHALL compute the admin ApplicationCredential OpenBao RemoteKey per-ControlPlane via a builder adminAppCredentialRemoteKeyFor(cp *c5c3v1alpha1.ControlPlane) returning `openstack/keystone/{cp.Namespace}/{cp.Name}/admin/app-credential`, replacing the package-level constant adminAppCredentialRemoteKey (reconcile_korc.go:58).",
      "priority": "SHALL",
      "rationale": "Boundary 1: the flat constant is the primary collision; two ControlPlanes mint distinct ACs to one path, last-writer-wins, K-ORC then authenticates with the wrong tenant's credential.",
      "scenarios": [
        {
          "name": "Builder embeds namespace and name",
          "when": "adminAppCredentialRemoteKeyFor is called with cp{Namespace: openstack, Name: controlplane}",
          "then": "it returns exactly \"openstack/keystone/openstack/controlplane/admin/app-credential\"",
          "and_then": [
            "both the Namespace and Name appear as distinct path segments",
            "the literal /admin/app-credential suffix is preserved"
          ]
        },
        {
          "name": "Distinct ControlPlanes yield distinct keys",
          "when": "the builder is called for openstack/cp-a and tenant-b/cp-b",
          "then": "the two returned RemoteKey strings are not equal",
          "and_then": [
            "neither collides with the legacy flat openstack/keystone/admin/app-credential"
          ]
        },
        {
          "name": "PushSecret builder threads the per-CR key",
          "when": "adminAppCredentialPushSecret(cp) builds the PushSecret",
          "then": "Spec.Data[0].Match.RemoteRef.RemoteKey == adminAppCredentialRemoteKeyFor(cp)",
          "and_then": [
            "ObjectMeta.Name stays adminAppCredentialPushSecretName(cp)",
            "DeletionPolicy stays None",
            "SecretStoreRef stays the openbao-cluster-store ClusterSecretStore"
          ]
        }
      ]
    },
    {
      "id": "REQ-002",
      "description": "The keystone-operator SHALL compute the admin-password backup RemoteKey per-Keystone-CR as `bootstrap/{keystone.Namespace}/{keystone.Name}/admin`, replacing the hardcoded \"bootstrap/keystone-admin\" literal (reconcile_passwordrotation.go:603).",
      "priority": "SHALL",
      "rationale": "Boundary 2: the flat bootstrap path means two Model-B Keystone CRs overwrite the same admin password; the keystone-admin ExternalSecret then resolves the wrong password.",
      "scenarios": [
        {
          "name": "Per-CR password path",
          "when": "adminPasswordPushSecret is called with keystone{Namespace: openstack, Name: controlplane-keystone}",
          "then": "RemoteKey == \"bootstrap/openstack/controlplane-keystone/admin\"",
          "and_then": [
            "Property stays adminPasswordSecretKey",
            "DeletionPolicy stays None"
          ]
        },
        {
          "name": "Same name different namespace stays isolated",
          "when": "two Keystone CRs both named 'keystone' exist in namespaces openstack and tenant-b",
          "then": "their RemoteKeys are bootstrap/openstack/keystone/admin and bootstrap/tenant-b/keystone/admin respectively",
          "and_then": [
            "the two values differ"
          ]
        },
        {
          "name": "Single-CR assumption text removed",
          "when": "keystone_types.go PasswordRotationSpec and the builder godoc are read after the change",
          "then": "no 'single Model-B-enabled Keystone CR per cluster' sentence remains",
          "and_then": [
            "a CC-0112 reference replaces it"
          ]
        }
      ]
    },
    {
      "id": "REQ-003",
      "description": "ensureKORCAdminImports SHALL create the imported K-ORC admin User with Kubernetes metadata.name = cp.Name + \"-user-admin\" (via adminUserRef), while keeping Spec.Import.Filter.Name = OpenStack username 'admin'.",
      "priority": "SHALL",
      "rationale": "Boundary 3: adminUserRef currently returns the cloudName ('admin'), so two ControlPlanes in one namespace fight over a single 'admin' User CR's ownerReferences; adminDomainRef already proves the cp.Name-prefix pattern.",
      "scenarios": [
        {
          "name": "User name is CR-scoped",
          "when": "ensureKORCAdminImports runs for cp{Name: controlplane}",
          "then": "a User CR named 'controlplane-user-admin' is created/updated in childNamespace(cp)",
          "and_then": [
            "it is owned by the ControlPlane via SetControllerReference"
          ]
        },
        {
          "name": "Inner OpenStack filter unchanged",
          "when": "the created User's Spec.Import.Filter is inspected",
          "then": "Filter.Name == OpenStackName('admin') and Filter.DomainRef == KubernetesNameRef(adminDomainRef(cp))",
          "and_then": [
            "the K-ORC import still resolves to the real OpenStack admin user"
          ]
        },
        {
          "name": "Two CRs, two Users",
          "when": "two ControlPlanes cp-a and cp-b are reconciled in the same namespace",
          "then": "two distinct User CRs 'cp-a-user-admin' and 'cp-b-user-admin' exist",
          "and_then": [
            "neither flaps the other's ownerReferences"
          ]
        }
      ]
    },
    {
      "id": "REQ-004",
      "description": "DECISION boundary 4 = option (a): the keystone-operator SHALL extend the fernet/credential RemoteKeys with a namespace segment to `openstack/keystone/{keystone.Namespace}/{keystone.Name}/{fernet,credential}-keys` (reconcile_fernet.go:455, reconcile_credential.go:459) and the Decision SHALL be recorded as a comment block at both call sites.",
      "priority": "SHALL",
      "rationale": "Boundary 4: CC-0093 made these per-CR-NAME only; two Keystone CRs with the same name in different namespaces still collide. Option (a) (namespace segment) is consistent with boundaries 1-2 and does not require a cluster-wide CR-name-uniqueness guard.",
      "scenarios": [
        {
          "name": "Fernet key gains namespace segment",
          "when": "fernetKeysPushSecret is called with keystone{Namespace: openstack, Name: controlplane-keystone}",
          "then": "RemoteKey == \"openstack/keystone/openstack/controlplane-keystone/fernet-keys\"",
          "and_then": [
            "the local Secret name {name}-fernet-keys and the PushSecret name are unchanged"
          ]
        },
        {
          "name": "Credential key gains namespace segment",
          "when": "credentialKeysPushSecret is called with the same keystone",
          "then": "RemoteKey == \"openstack/keystone/openstack/controlplane-keystone/credential-keys\"",
          "and_then": []
        },
        {
          "name": "Decision recorded at call site",
          "when": "reconcile_fernet.go:455 and reconcile_credential.go:459 are read",
          "then": "a comment block records 'DECISION boundary 4 (CC-0112): option (a) namespace segment' with the rationale",
          "and_then": [
            "the godoc references both keystone.Namespace and keystone.Name"
          ]
        }
      ]
    },
    {
      "id": "REQ-005",
      "description": "The push-app-credentials.hcl OpenBao policy SHALL grant create/update/read on kv-v2/data and kv-v2/metadata for the template `openstack/keystone/+/+/admin/app-credential`, replacing the literal leaf, and SHALL NOT widen access beyond that two-segment template.",
      "priority": "SHALL",
      "rationale": "Boundary 1 ACL: the writer's new per-CR path must be authorised; the +/+ glob is the minimal widening that still excludes the db and fernet/credential leaves.",
      "scenarios": [
        {
          "name": "Two-segment glob granted",
          "when": "the policy is loaded into OpenBao and inspected with `bao policy read`",
          "then": "both kv-v2/data/openstack/keystone/+/+/admin/app-credential and the metadata twin are present with [create,update,read]",
          "and_then": [
            "the literal openstack/keystone/admin/app-credential leaf is gone"
          ]
        },
        {
          "name": "No db lock-out",
          "when": "a token bound to the policy attempts to write kv-v2/data/openstack/keystone/openstack/db",
          "then": "the write is denied (the /admin/app-credential suffix is required)",
          "and_then": [
            "the fernet/credential leaves are likewise not writable via this policy"
          ]
        },
        {
          "name": "Header documents the change",
          "when": "the policy header is read",
          "then": "it cites CC-0112, explains the namespace+name +/+ segments, and the single-CR justification is removed",
          "and_then": [
            "the metadata-path rationale block is preserved"
          ]
        }
      ]
    },
    {
      "id": "REQ-006",
      "description": "The push-keystone-admin.hcl OpenBao policy SHALL grant create/update/read/delete on kv-v2/data and kv-v2/metadata for the template `bootstrap/+/+/admin`, replacing the two literal bootstrap/keystone-admin leaves, and the single-CR justification header SHALL be rewritten.",
      "priority": "SHALL",
      "rationale": "Boundary 2 ACL: the per-CR password writer must be authorised without granting a write wildcard over the whole bootstrap subtree (which would expose database root credentials).",
      "scenarios": [
        {
          "name": "Two-segment bootstrap glob",
          "when": "the policy is loaded and inspected",
          "then": "kv-v2/data/bootstrap/+/+/admin and its metadata twin are granted [create,update,read,delete]",
          "and_then": [
            "no trailing-* over bootstrap/* is introduced"
          ]
        },
        {
          "name": "Sibling bootstrap secrets stay protected",
          "when": "a policy-bound token attempts to write kv-v2/data/bootstrap/infrastructure/mariadb",
          "then": "the write is denied (the /admin suffix and exactly two mid-segments are required)",
          "and_then": []
        },
        {
          "name": "Header rewritten",
          "when": "the header (former lines 18-29) is read",
          "then": "the 'at most one Model-B-enabled Keystone CR per cluster' justification is gone and replaced with the per-namespace/per-name semantics and a CC-0112 reference",
          "and_then": []
        }
      ]
    },
    {
      "id": "REQ-007",
      "description": "DECISION boundary 4 ACL: push-keystone-keys.hcl SHALL be widened from `openstack/keystone/+/{fernet,credential}-keys` to `openstack/keystone/+/+/{fernet,credential}-keys` on both data and metadata paths, keeping the /db exclusion rationale.",
      "priority": "SHALL",
      "rationale": "The namespace segment added in REQ-004 needs one more glob segment in the policy; the existing CC-0093 +-glob citation already validates the syntax.",
      "scenarios": [
        {
          "name": "Two-segment fernet/credential globs",
          "when": "the policy is inspected after the change",
          "then": "all four paths use +/+/{fernet,credential}-keys with [create,update,read,delete]",
          "and_then": [
            "the data and metadata twins both exist"
          ]
        },
        {
          "name": "db remains unwritable",
          "when": "the /db exclusion paragraph is re-validated",
          "then": "kv-v2/data/openstack/keystone/<ns>/db is still outside the match set (the /fernet-keys or /credential-keys suffix is required)",
          "and_then": []
        },
        {
          "name": "Citation retained",
          "when": "the header is read",
          "then": "the upstream OpenBao +-glob single-segment citation is retained and the CC-0112 namespace-segment note is added",
          "and_then": []
        }
      ]
    },
    {
      "id": "REQ-008",
      "description": "The eso-management.hcl read-side wildcard SHALL be verified to still cover the new per-CR shapes on read; if `kv-v2/data/openstack/keystone/*` does not cover the added namespace segment it SHALL be widened, and the bootstrap-read path SHALL cover bootstrap/+/+/admin.",
      "priority": "SHALL",
      "rationale": "Boundary 1/2 read side: the management-cluster ESO role reads via eso-management; a too-narrow read glob would 403 the new paths and never materialise the consumer Secrets.",
      "scenarios": [
        {
          "name": "Keystone subtree read coverage",
          "when": "the trailing kv-v2/data/openstack/keystone/* grant is evaluated against openstack/keystone/openstack/controlplane/admin/app-credential",
          "then": "the path is covered (trailing * spans descendant segments) OR the grant is widened to cover it",
          "and_then": [
            "a comment records the verification result and CC-0112"
          ]
        },
        {
          "name": "Bootstrap read coverage",
          "when": "the kv-v2/data/bootstrap/* grant is evaluated against bootstrap/openstack/controlplane-keystone/admin",
          "then": "the path is covered by the trailing *",
          "and_then": []
        },
        {
          "name": "No write capability added",
          "when": "eso-management.hcl is re-read after any change",
          "then": "it remains read-only (write stays in the push-* policies)",
          "and_then": []
        }
      ]
    },
    {
      "id": "REQ-009",
      "description": "DECISION boundary 5 = option (a): write-bootstrap-secrets.sh SHALL accept a list of ControlPlane identities (e.g. KORC_CONTROLPLANES=\"<ns>/<cp> ...\", default = the single canonical identity) and loop the K-ORC clouds.yaml seed plus mark_eso_managed per identity, writing the per-CR paths; it SHALL NOT silently keep seeding the legacy flat paths on a multi-CR cluster.",
      "priority": "SHALL",
      "rationale": "Boundary 5: the seed is single-tenant; option (a) keeps the cluster-bootstrap path and is independent of #412, while satisfying the 'no silent legacy flat seed' acceptance criterion.",
      "scenarios": [
        {
          "name": "Default single identity unchanged in behaviour",
          "when": "the script runs with no KORC_CONTROLPLANES override on a single-CR cluster",
          "then": "it seeds exactly the canonical per-CR admin AC path and the canonical per-CR bootstrap/admin path (derived from the default identity) and marks each eso-managed",
          "and_then": [
            "a single-CR deploy continues to work end-to-end"
          ]
        },
        {
          "name": "Multiple identities loop",
          "when": "KORC_CONTROLPLANES=\"openstack/controlplane tenant-b/controlplane\" is set",
          "then": "the script seeds one openstack/keystone/<ns>/<cp>/admin/app-credential and one bootstrap/<ns>/<keystone>/admin per identity",
          "and_then": [
            "each seeded path is mark_eso_managed",
            "idempotent re-runs do not clobber minted credentials"
          ]
        },
        {
          "name": "No legacy flat seed",
          "when": "the script is grepped after the change",
          "then": "no unconditional write to the flat kv-v2/openstack/keystone/admin/app-credential or kv-v2/bootstrap/keystone-admin remains",
          "and_then": [
            "set -euo pipefail and the in-pod generation discipline are preserved"
          ]
        }
      ]
    },
    {
      "id": "REQ-010",
      "description": "DECISION boundary 6 = option (a): ControlPlaneWebhook.ValidateCreate SHALL reject a ControlPlane when another ControlPlane already exists in the same namespace (using the injected Client to List), and CredentialRotationReconciler.resolveControlPlane SHALL keep the AmbiguousControlPlane branch as a defense-in-depth fallback. No CRD field or schema version SHALL be added.",
      "priority": "SHALL",
      "rationale": "Boundary 6: resolveControlPlane already assumes one CP per namespace (client.InNamespace); enforcing it at admission is the lowest-risk path and matches the 'two namespaces' multi-instance framing without a CRD change.",
      "scenarios": [
        {
          "name": "Second ControlPlane rejected",
          "when": "a ControlPlane already exists in namespace openstack and a second create is attempted there",
          "then": "ValidateCreate returns an Invalid/Forbidden error naming the existing ControlPlane",
          "and_then": [
            "the message explains the one-ControlPlane-per-namespace contract"
          ]
        },
        {
          "name": "First create and updates admitted",
          "when": "the first ControlPlane in a namespace is created, or an existing one is updated",
          "then": "admission succeeds (ValidateUpdate does not run the count check)",
          "and_then": [
            "the existing 5 validation rules still run"
          ]
        },
        {
          "name": "AmbiguousControlPlane kept as fallback",
          "when": "resolveControlPlane is read after the change",
          "then": "the >1 case still returns Reason AmbiguousControlPlane (now unreachable when the webhook is active)",
          "and_then": [
            "the docs describe it as defense-in-depth, not the primary signal"
          ]
        }
      ]
    },
    {
      "id": "REQ-011",
      "description": "The static ESO reader manifests SHALL be repointed to the per-CR keys for the default deployment identity so the single-CR happy path does not regress: keystone-admin.yaml -> bootstrap/{ns}/{keystone}/admin (boundary 2 reader, in #414 scope), and k-orc-clouds-yaml.yaml -> openstack/keystone/{ns}/{cp}/admin/app-credential (interim shim; full per-CR templating is #412).",
      "priority": "SHALL",
      "rationale": "Boundary 7 + the 'happy path must not regress' acceptance criterion: the writer's path changes here, so any reader baked into a static manifest must change with it or K-ORC/Keystone cannot authenticate.",
      "scenarios": [
        {
          "name": "keystone-admin reader matches writer",
          "when": "deploy/eso/externalsecrets/keystone-admin.yaml is read after the change",
          "then": "remoteRef.key == the per-CR bootstrap/{ns}/{keystone}/admin for the deployment's Keystone CR identity",
          "and_then": [
            "the property stays 'password'"
          ]
        },
        {
          "name": "k-orc-clouds-yaml reader matches writer",
          "when": "both ExternalSecrets in k-orc-clouds-yaml.yaml are read",
          "then": "remoteRef.key == openstack/keystone/{ns}/{cp}/admin/app-credential for the default ControlPlane identity",
          "and_then": [
            "a comment notes #412 supersedes this static shim with operator-owned per-CR templating"
          ]
        },
        {
          "name": "Identity verified against the e2e fixture",
          "when": "the repointed literals are cross-checked against tests/e2e/c5c3/full-controlplane-keystone fixtures and deploy/kind/controlplane/controlplane.yaml",
          "then": "the keys match the ControlPlane 'controlplane' and projected Keystone 'controlplane-keystone' identities exactly",
          "and_then": []
        }
      ]
    },
    {
      "id": "REQ-012",
      "description": "A new E2E suite under tests/e2e/c5c3/multi-controlplane/ SHALL bring up two ControlPlane CRs in two namespaces, assert both reach Ready=True with distinct status.adminApplicationCredential.id, rotate one CR's admin AC via CredentialRotation, and assert the other CR's AC is unaffected.",
      "priority": "SHALL",
      "rationale": "Acceptance criterion: the multi-instance guarantee must be protected at the integration boundary (chainsaw + real OpenBao), not only at the builder level.",
      "scenarios": [
        {
          "name": "Both tenants reach Ready",
          "when": "ControlPlanes are applied in tenant-a and tenant-b namespaces",
          "then": "both reach Ready=True with distinct status.adminApplicationCredential.id",
          "and_then": [
            "the two OpenBao admin AC paths both exist and differ (verified via kubectl exec bao kv get)"
          ]
        },
        {
          "name": "Rotation isolation",
          "when": "the admin AC of tenant-a is rotated via CredentialRotation",
          "then": "tenant-b's status.adminApplicationCredential.lastRotation is unchanged and its AC stays Available=True throughout",
          "and_then": []
        },
        {
          "name": "Suite follows chainsaw conventions",
          "when": "the chainsaw-test.yaml is read",
          "then": "it uses the same Test kind, namespaced steps, try/assert and kubectl-exec-into-openbao idioms as concurrent-cr-conflicts and full-controlplane-keystone",
          "and_then": []
        }
      ]
    },
    {
      "id": "REQ-013",
      "description": "The existing tests/e2e/c5c3/full-controlplane-keystone/ chainsaw test SHALL continue to pass against the per-CR path layout, with any sub-asserts that reference the admin AC OpenBao path updated to the per-CR key.",
      "priority": "SHALL",
      "rationale": "No-regression guarantee for the single-CR happy path; the writer/reader agreement must hold for the default identity.",
      "scenarios": [
        {
          "name": "Sub-asserts updated",
          "when": "full-controlplane-keystone/chainsaw-test.yaml is grepped for the flat openstack/keystone/admin/app-credential",
          "then": "any direct OpenBao-path assertion is updated to openstack/keystone/openstack/controlplane/admin/app-credential",
          "and_then": [
            "assertions that reference only the k-orc-clouds-yaml Secret name are unaffected"
          ]
        },
        {
          "name": "PushSecret name unchanged",
          "when": "the test's PUSH_SECRET binding (${CP_NAME}-admin-app-credential-backup) is inspected",
          "then": "it is unchanged (only the remote KV path changed, not the PushSecret object name)",
          "and_then": []
        },
        {
          "name": "Happy path green",
          "when": "the suite is run (or statically validated if no cluster is available)",
          "then": "the ControlPlane reaches Ready=True and k-orc-clouds-yaml materialises",
          "and_then": [
            "any sandbox no-cluster limitation is reported honestly"
          ]
        }
      ]
    },
    {
      "id": "REQ-014",
      "description": "The reference documentation (controlplane-reconciler.md, controlplane-crd.md, keystone-reconciler.md, infrastructure-manifests.md) SHALL describe the per-CR path layout and the one-ControlPlane-per-namespace contract, and SHALL remove the 'single Model-B-enabled Keystone CR per cluster' / 'single admin AC per cluster' wording.",
      "priority": "SHALL",
      "rationale": "Docs-vs-code drift: the single-CR caveats become false once the per-CR layout lands; the AmbiguousControlPlane row must reflect the admission guard.",
      "scenarios": [
        {
          "name": "Per-CR paths in docs",
          "when": "the four reference docs are read after the change",
          "then": "every documented occurrence of the flat admin AC / bootstrap paths shows the per-CR template",
          "and_then": [
            "a new Multi-instance section is added to controlplane-reconciler.md"
          ]
        },
        {
          "name": "AmbiguousControlPlane row updated",
          "when": "the CredentialRotation reconciler table in controlplane-reconciler.md is read",
          "then": "the AmbiguousControlPlane row describes the webhook guard + defense-in-depth fallback",
          "and_then": []
        },
        {
          "name": "Single-CR wording removed",
          "when": "the docs and source comments are grepped for 'single Model-B-enabled Keystone CR per cluster' and 'single admin AC'",
          "then": "no such assertion remains (in code comments, policy headers, or docs)",
          "and_then": []
        }
      ]
    },
    {
      "id": "REQ-015",
      "description": "A Migration runbook (a Migration section in controlplane-reconciler.md and keystone-reconciler.md, mirroring the CC-0093 migration-note discipline) SHALL document the one-time copy of the legacy flat leaves into the per-CR layout, the ACL re-apply, the operator-upgrade ordering, and the bao kv metadata delete purge of the orphaned legacy leaves.",
      "priority": "SHALL",
      "rationale": "Boundary 8: brownfield clusters hold one legacy leaf each; without a copy step the readers point at paths the new writers no longer populate.",
      "scenarios": [
        {
          "name": "Copy commands given",
          "when": "the Migration section is read",
          "then": "it gives bao kv get (legacy) -> bao kv put (per-CR) commands for the admin AC and admin password leaves and the boundary-4 fernet/credential leaves",
          "and_then": [
            "the per-CR path placeholders are explicit"
          ]
        },
        {
          "name": "Ordering documented",
          "when": "the runbook order is read",
          "then": "it states: copy legacy->new, re-apply the three ACLs, then upgrade the operators",
          "and_then": [
            "the 'orphaned but harmless' wording mirrors CC-0093"
          ]
        },
        {
          "name": "Purge step",
          "when": "the cleanup step is read",
          "then": "bao kv metadata delete is given for kv-v2/openstack/keystone/admin/app-credential and kv-v2/bootstrap/keystone-admin",
          "and_then": [
            "it notes the operator never writes the legacy leaves"
          ]
        }
      ]
    },
    {
      "id": "REQ-016",
      "description": "The change SHALL NOT modify the architecture/ git submodule from this worktree; the architecture-level update SHALL be flagged in the PR description as a separate upstream PR, mirroring CC-0093 REQ-008.",
      "priority": "SHALL",
      "rationale": "architecture/ is a submodule (read-only from the worktree); cross-context writes produce inconsistent state and are reverted.",
      "scenarios": [
        {
          "name": "No submodule writes",
          "when": "git status / git submodule status is checked after the change",
          "then": "no file under architecture/ is modified",
          "and_then": [
            "all edits land in the parent repo only"
          ]
        },
        {
          "name": "Upstream PR flagged",
          "when": "the PR description is read",
          "then": "it flags the architecture/docs/09-implementation change as a follow-up upstream PR",
          "and_then": []
        },
        {
          "name": "Source-of-truth note",
          "when": "any architecture mention of the flat paths is identified",
          "then": "it is recorded as a deferred upstream change, not edited here",
          "and_then": []
        }
      ]
    }
  ],
  "tasks": [
    {
      "id": "1.1",
      "title": "In operators/c5c3/internal/controller/reconcile_korc.go replace the const adminAppCredentialRemoteKey (line 58) with a builder adminAppCredentialRemoteKeyFor(cp *c5c3v1alpha1.ControlPlane) string returning fmt.Sprintf(\"openstack/keystone/%s/%s/admin/app-credential\", cp.Namespace, cp.Name); update its godoc to drop the 'single admin AC per cluster' sentence and cite CC-0112. (REQ-001)",
      "level": 1,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-001"
      ]
    },
    {
      "id": "1.2",
      "title": "Update adminAppCredentialPushSecret (reconcile_korc.go:1066-1092) line 1086 to RemoteKey: adminAppCredentialRemoteKeyFor(cp) and update the builder godoc (lines 1058-1060) to describe the per-CR path; keep DeletionPolicy=None and the cluster-store ref. (REQ-001)",
      "level": 1,
      "estimate_minutes": 10,
      "requirements": [
        "REQ-001"
      ]
    },
    {
      "id": "1.3",
      "title": "Change adminUserRef (reconcile_korc.go:666-671) to return fmt.Sprintf(\"%s-user-admin\", cp.Name) mirroring adminDomainRef; keep ensureKORCAdminImports' User.Spec.Import.Filter.Name = OpenStackName(korcAdminUsername) ('admin') unchanged; update the godoc to explain the K8s-name vs OpenStack-name split (CC-0112). (REQ-003)",
      "level": 1,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-003"
      ]
    },
    {
      "id": "1.4",
      "title": "Update reconcile_korc_test.go: replace every literal 'openstack/keystone/admin/app-credential' assertion (lines 81,220,373,389,696,720,878,1012,1083 and the 485-486 RemoteKey assert) with To(Equal(adminAppCredentialRemoteKeyFor(cp))); add TestAdminAppCredentialRemoteKeyFor_EmbedsNamespaceAndName (table over two CPs). (REQ-001)",
      "level": 1,
      "estimate_minutes": 25,
      "requirements": [
        "REQ-001"
      ]
    },
    {
      "id": "1.5",
      "title": "Update reconcile_korc_test.go TestEnsureKORCAdminImports_CreatesUnmanagedUserAndDomain: assert User name Equal(cp.Name+\"-user-admin\") (lines 1098-1099) and keep Filter.Name Equal(\"admin\") (line 1105); add TestAdminUserRef_IsControlPlaneScoped. (REQ-003)",
      "level": 1,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-003"
      ]
    },
    {
      "id": "2.1",
      "title": "In operators/keystone/internal/controller/reconcile_passwordrotation.go:603 replace RemoteKey: \"bootstrap/keystone-admin\" with fmt.Sprintf(\"bootstrap/%s/%s/admin\", keystone.Namespace, keystone.Name); rewrite the builder godoc (lines 568-581) to drop the single-CR assumption and cite CC-0112. (REQ-002)",
      "level": 1,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-002"
      ]
    },
    {
      "id": "2.2",
      "title": "Delete the 'Model B assumes a single Model-B-enabled Keystone CR per cluster' comment block in operators/keystone/api/v1alpha1/keystone_types.go:403-407 and replace with a per-CR note referencing CC-0112. (REQ-002, REQ-014)",
      "level": 1,
      "estimate_minutes": 10,
      "requirements": [
        "REQ-002",
        "REQ-014"
      ]
    },
    {
      "id": "2.3",
      "title": "Update reconcile_passwordrotation_test.go:450 to Equal(fmt.Sprintf(\"bootstrap/%s/%s/admin\", ks.Namespace, ks.Name)) and update integration_test.go:4637 (and its CC-0109 comment); add TestAdminPasswordPushSecret_RemoteKeyIsPerCR (two same-name CRs in different namespaces). (REQ-002)",
      "level": 1,
      "estimate_minutes": 25,
      "requirements": [
        "REQ-002"
      ]
    },
    {
      "id": "2.4",
      "title": "DECISION boundary 4 = option (a): in reconcile_fernet.go:455 and reconcile_credential.go:459 add the keystone.Namespace segment -> \"openstack/keystone/\"+keystone.Namespace+\"/\"+keystone.Name+\"/{fernet,credential}-keys\"; record a DECISION comment block (boundary 4, CC-0112) at both call sites and update the godoc. (REQ-004)",
      "level": 1,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-004"
      ]
    },
    {
      "id": "2.5",
      "title": "Update reconcile_fernet_test.go:414 and reconcile_credential_test.go:418 to the namespace-segmented RemoteKey; add TestFernetKeysPushSecret_RemoteKeyIsNamespaceAndNameScoped (two same-name CRs in different namespaces, distinct keys). (REQ-004)",
      "level": 1,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-004"
      ]
    },
    {
      "id": "3.1",
      "title": "DECISION boundary 6 = option (a): in operators/c5c3/api/v1alpha1/controlplane_webhook.go un-blank the ctx in ValidateCreate and add a one-ControlPlane-per-namespace check using w.Client.List(ctx, &ControlPlaneList, client.InNamespace(obj.Namespace)); reject when len>=1 with field.Forbidden naming the existing CR; do NOT run the check in ValidateUpdate; record the Decision in the validate godoc (CC-0112). (REQ-010)",
      "level": 2,
      "estimate_minutes": 25,
      "requirements": [
        "REQ-010"
      ]
    },
    {
      "id": "3.2",
      "title": "Verify ControlPlaneWebhook.Client is wired (non-nil) at operator startup (cmd/main wiring); if it is only set to a Reader that lacks List in tests, ensure the controller-runtime client is injected. Add a short DECISION comment if the wiring is confirmed already present. (REQ-010)",
      "level": 2,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-010"
      ]
    },
    {
      "id": "3.3",
      "title": "Add controlplane_webhook_test.go tests TestValidateCreate_RejectsSecondControlPlaneInNamespace and TestValidateCreate_AllowsFirstControlPlane_AndUpdate using fake.NewClientBuilder() seeded with an existing ControlPlane; keep the 5 existing rule tests green. (REQ-010)",
      "level": 2,
      "estimate_minutes": 25,
      "requirements": [
        "REQ-010"
      ]
    },
    {
      "id": "3.4",
      "title": "In reconcile_credentialrotation.go:198-230 keep the AmbiguousControlPlane branch but update its comment to mark it defense-in-depth (unreachable when the webhook guard is active); add/adjust TestResolveControlPlane_AmbiguousIsDefenseInDepth in reconcile_credentialrotation_test.go. (REQ-010)",
      "level": 2,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-010"
      ]
    },
    {
      "id": "4.1",
      "title": "Rewrite deploy/openbao/policies/push-app-credentials.hcl: replace the literal openstack/keystone/admin/app-credential data+metadata blocks (lines 34-40) with kv-v2/{data,metadata}/openstack/keystone/+/+/admin/app-credential [create,update,read]; rewrite the header (lines 14-32) to document the namespace+name +/+ segments, keep the metadata-path rationale, cite CC-0112, drop the single-CR justification. (REQ-005)",
      "level": 2,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-005"
      ]
    },
    {
      "id": "4.2",
      "title": "Rewrite deploy/openbao/policies/push-keystone-admin.hcl: replace the two literal bootstrap/keystone-admin leaves (lines 54-60) with kv-v2/{data,metadata}/bootstrap/+/+/admin [create,update,read,delete]; rewrite the header (lines 18-29) to remove the single-CR justification and document the two-segment-glob + sibling-bootstrap-protection rationale, cite CC-0112. (REQ-006)",
      "level": 2,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-006"
      ]
    },
    {
      "id": "4.3",
      "title": "Widen deploy/openbao/policies/push-keystone-keys.hcl (lines 73-87) from +/{fernet,credential}-keys to +/+/{fernet,credential}-keys on data+metadata; update the header (lines 14-56) to add the CC-0112 namespace segment while retaining the upstream +-glob citation and the /db exclusion paragraph. (REQ-007)",
      "level": 2,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-007"
      ]
    },
    {
      "id": "4.4",
      "title": "Verify deploy/openbao/policies/eso-management.hcl read coverage: confirm kv-v2/data/openstack/keystone/* and kv-v2/data/bootstrap/* trailing wildcards cover the new per-CR shapes; widen and add a CC-0112 verification comment only if not covered; keep the policy read-only. (REQ-008)",
      "level": 2,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-008"
      ]
    },
    {
      "id": "5.1",
      "title": "Repoint deploy/eso/externalsecrets/keystone-admin.yaml remoteRef.key (line 27) to the per-CR bootstrap/{ns}/{keystone}/admin for the default deployment's Keystone identity (verify against the e2e/Quick Start fixture); keep property: password; add a CC-0112 comment. (REQ-011)",
      "level": 3,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-011"
      ]
    },
    {
      "id": "5.2",
      "title": "Repoint both ExternalSecrets in deploy/eso/externalsecrets/k-orc-clouds-yaml.yaml remoteRef.key (lines 49,69) to openstack/keystone/openstack/controlplane/admin/app-credential (default ControlPlane identity from deploy/kind/controlplane/controlplane.yaml); add a comment that #412 supersedes this static shim with operator-owned per-CR templating. (REQ-011)",
      "level": 3,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-011"
      ]
    },
    {
      "id": "5.3",
      "title": "Refactor deploy/openbao/bootstrap/write-bootstrap-secrets.sh (boundary 5 option a): add a KORC_CONTROLPLANES list (default = the single canonical '<ns>/<cp>' identity), loop seed_korc_bootstrap_clouds_yaml + mark_eso_managed per identity writing openstack/keystone/{ns}/{cp}/admin/app-credential, and loop the bootstrap/{ns}/{keystone}/admin seed; remove the unconditional legacy flat writes; keep set -euo pipefail and in-pod generation. (REQ-009)",
      "level": 3,
      "estimate_minutes": 30,
      "requirements": [
        "REQ-009"
      ]
    },
    {
      "id": "5.4",
      "title": "Trace callers of write-bootstrap-secrets.sh (hack/deploy-infra.sh ~line 814, setup-policies.sh) and confirm KORC_KEYSTONE_AUTH_URL and the new KORC_CONTROLPLANES default keep the single-CR deploy path unchanged; add a DECISION comment documenting the default identity. (REQ-009)",
      "level": 3,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-009"
      ]
    },
    {
      "id": "6.1",
      "title": "Extend operators/c5c3/internal/controller/integration_test.go with TestIntegration_MultiControlPlane_DistinctAdminCredentialPaths: bring up two ControlPlanes in two namespaces (reuse setupControlPlaneEnvTest + integrationManagedControlPlane), drive both to AdminCredentialReady, assert distinct PushSecret RemoteKeys (containing each Namespace+Name) and distinct User CR names. (REQ-012)",
      "level": 4,
      "estimate_minutes": 30,
      "requirements": [
        "REQ-012"
      ]
    },
    {
      "id": "6.2",
      "title": "Confirm the existing single-ControlPlane envtest path (TestIntegration_SingleControlPlane_AdminCredentialReady or equivalent) still drives AdminCredentialReady=True with the per-CR RemoteKey; adjust any assertion pinning the flat path. (REQ-013)",
      "level": 4,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-013"
      ]
    },
    {
      "id": "6.3",
      "title": "In operators/keystone/internal/controller/integration_test.go extend TestIntegrationKeystone_PushSecretRemoteKeyIsPerCR (lines 3799-3866) to also cover two CRs of the SAME name in DIFFERENT namespaces, asserting the namespace-segmented fernet/credential and bootstrap/admin RemoteKeys are disjoint. (REQ-004, REQ-002)",
      "level": 4,
      "estimate_minutes": 25,
      "requirements": [
        "REQ-004",
        "REQ-002"
      ]
    },
    {
      "id": "7.1",
      "title": "Create tests/e2e/c5c3/multi-controlplane/ with ControlPlane fixtures for tenant-a and tenant-b namespaces and a chainsaw-test.yaml that creates both namespaces+CRs and asserts both reach Ready=True with distinct status.adminApplicationCredential.id (mirror full-controlplane-keystone + concurrent-cr-conflicts idioms). (REQ-012)",
      "level": 5,
      "estimate_minutes": 30,
      "requirements": [
        "REQ-012"
      ]
    },
    {
      "id": "7.2",
      "title": "Add a multi-controlplane chainsaw step that kubectl-execs bao kv get for both openstack/keystone/{ns}/{cp}/admin/app-credential paths (asserting both exist and differ), rotates tenant-a's admin AC via CredentialRotation, and asserts tenant-b status.adminApplicationCredential.lastRotation is unchanged and its AC stays Available=True. (REQ-012)",
      "level": 5,
      "estimate_minutes": 30,
      "requirements": [
        "REQ-012"
      ]
    },
    {
      "id": "7.3",
      "title": "Update tests/e2e/c5c3/full-controlplane-keystone/chainsaw-test.yaml: change any direct OpenBao-path sub-assert (around lines 95-103, 227-237) to openstack/keystone/openstack/controlplane/admin/app-credential; keep the ${CP_NAME}-admin-app-credential-backup PushSecret-name binding unchanged. (REQ-013)",
      "level": 5,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-013"
      ]
    },
    {
      "id": "7.4",
      "title": "Update tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml (lines 63-173) so the four asserted per-CR kv-v2 paths use the namespace-segmented openstack/keystone/{ns}/{name}/{fernet,credential}-keys shape; confirm the deletion-cleanup test (if it asserts these paths) is consistent. (REQ-007)",
      "level": 5,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-007"
      ]
    },
    {
      "id": "8.1",
      "title": "Update docs/reference/c5c3/controlplane-reconciler.md (lines ~507,519,529,641) to the per-CR admin AC remote key; update the AmbiguousControlPlane row (~609) to describe the webhook guard + defense-in-depth fallback; add a 'Multi-instance' section explaining the per-CR path layout and the one-ControlPlane-per-namespace contract. (REQ-014)",
      "level": 6,
      "estimate_minutes": 30,
      "requirements": [
        "REQ-014"
      ]
    },
    {
      "id": "8.2",
      "title": "Update docs/reference/c5c3/controlplane-crd.md (around line 317) for the CloudCredentialsRef.SecretName default note and add a Status/Multi-instance note that the admin AC OpenBao path is per-ControlPlane (namespace+name). (REQ-014)",
      "level": 6,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-014"
      ]
    },
    {
      "id": "8.3",
      "title": "Update docs/reference/keystone/keystone-reconciler.md Model B admin-password PushSecret table (lines ~2728-2744) to bootstrap/{ns}/{name}/admin and remove the single-CR caveat (~2744); extend the existing CC-0093 migration note (lines ~863-902) to cover the admin-password path. (REQ-014, REQ-015)",
      "level": 6,
      "estimate_minutes": 25,
      "requirements": [
        "REQ-014",
        "REQ-015"
      ]
    },
    {
      "id": "8.4",
      "title": "Update docs/reference/infrastructure/infrastructure-manifests.md (lines ~687,699,704-705) to describe the +/+ admin-app-credential and bootstrap/+/+/admin policy templates and the per-CR read keys. (REQ-014)",
      "level": 6,
      "estimate_minutes": 15,
      "requirements": [
        "REQ-014"
      ]
    },
    {
      "id": "8.5",
      "title": "Add a Migration section (in controlplane-reconciler.md, cross-linked from keystone-reconciler.md) giving the one-time bao kv get (legacy) -> bao kv put (per-CR) copy for openstack/keystone/admin/app-credential and bootstrap/keystone-admin and the boundary-4 fernet/credential leaves, the ACL re-apply, the operator-upgrade order, and the bao kv metadata delete purge; mirror CC-0093 'orphaned but harmless' wording. (REQ-015)",
      "level": 6,
      "estimate_minutes": 30,
      "requirements": [
        "REQ-015"
      ]
    },
    {
      "id": "8.6",
      "title": "Sweep: rg 'openstack/keystone/admin/app-credential|bootstrap/keystone-admin|single Model-B-enabled Keystone CR per cluster|single admin AC' across operators/ deploy/ docs/ and confirm every remaining match is a migration-note legacy reference; do NOT edit architecture/ (submodule); flag the architecture/docs/09-implementation update as a separate upstream PR in the PR description. (REQ-014, REQ-016)",
      "level": 6,
      "estimate_minutes": 20,
      "requirements": [
        "REQ-014",
        "REQ-016"
      ]
    }
  ],
  "test_specifications": [
    {
      "test_file": "operators/c5c3/internal/controller/reconcile_korc_test.go",
      "test_function": "TestAdminAppCredentialRemoteKeyFor_EmbedsNamespaceAndName",
      "story": "Operator engineer runs two ControlPlanes in two namespaces without OpenBao collisions",
      "expected": "[unit] Table over [openstack/controlplane, tenant-b/cp-b]; asserts builder returns openstack/keystone/{ns}/{name}/admin/app-credential and the two results differ",
      "requirement_id": "REQ-001"
    },
    {
      "test_file": "operators/c5c3/internal/controller/reconcile_korc_test.go",
      "test_function": "TestReconcileAdminCredential_PushSecretBuiltAndReady",
      "story": "Operator engineer runs two ControlPlanes in two namespaces without OpenBao collisions",
      "expected": "[unit] Replace the literal assertion at lines 485-486 with To(Equal(adminAppCredentialRemoteKeyFor(cp))); update the literal-path comments at 374/402",
      "requirement_id": "REQ-001"
    },
    {
      "test_file": "operators/keystone/internal/controller/reconcile_passwordrotation_test.go",
      "test_function": "TestAdminPasswordPushSecret_Shape",
      "story": "Keystone operator scopes the admin-password backup path per Keystone CR",
      "expected": "[unit] Update line 450 from Equal(\"bootstrap/keystone-admin\") to Equal(fmt.Sprintf(\"bootstrap/%s/%s/admin\", ks.Namespace, ks.Name))",
      "requirement_id": "REQ-002"
    },
    {
      "test_file": "operators/keystone/internal/controller/reconcile_passwordrotation_test.go",
      "test_function": "TestAdminPasswordPushSecret_RemoteKeyIsPerCR",
      "story": "Keystone operator scopes the admin-password backup path per Keystone CR",
      "expected": "[unit] New table over two CRs of same name in different namespaces; asserts distinct per-CR RemoteKeys",
      "requirement_id": "REQ-002"
    },
    {
      "test_file": "operators/c5c3/internal/controller/reconcile_korc_test.go",
      "test_function": "TestEnsureKORCAdminImports_CreatesUnmanagedUserAndDomain",
      "story": "Operator engineer gets distinct K-ORC User objects per ControlPlane",
      "expected": "[unit] Update the User name assertion (lines 1098-1099) to Equal(cp.Name+\"-user-admin\"); keep Filter.Name assertion Equal(\"admin\") at line 1105",
      "requirement_id": "REQ-003"
    },
    {
      "test_file": "operators/c5c3/internal/controller/reconcile_korc_test.go",
      "test_function": "TestAdminUserRef_IsControlPlaneScoped",
      "story": "Operator engineer gets distinct K-ORC User objects per ControlPlane",
      "expected": "[unit] New: adminUserRef(cp{Name:cp-a}) == \"cp-a-user-admin\" and differs from cp-b; asserts the prefix mirrors adminDomainRef",
      "requirement_id": "REQ-003"
    },
    {
      "test_file": "operators/keystone/internal/controller/reconcile_fernet_test.go",
      "test_function": "TestReconcileFernetKeys_PushSecretRemoteKey",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[unit] Update line 414 to Equal(\"openstack/keystone/{ns}/{name}/fernet-keys\") for the test CR's namespace/name",
      "requirement_id": "REQ-004"
    },
    {
      "test_file": "operators/keystone/internal/controller/reconcile_credential_test.go",
      "test_function": "TestReconcileCredentialKeys_PushSecretRemoteKey",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[unit] Update line 418 to Equal(\"openstack/keystone/{ns}/{name}/credential-keys\")",
      "requirement_id": "REQ-004"
    },
    {
      "test_file": "operators/keystone/internal/controller/reconcile_fernet_test.go",
      "test_function": "TestFernetKeysPushSecret_RemoteKeyIsNamespaceAndNameScoped",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[unit] New table over two same-name CRs in different namespaces; asserts distinct fernet RemoteKeys",
      "requirement_id": "REQ-004"
    },
    {
      "test_file": "tests/e2e/c5c3/multi-controlplane/chainsaw-test.yaml",
      "test_function": "step:verify-push-app-credentials-policy",
      "story": "Security reviewer confirms OpenBao ACLs stay least-privilege under the per-CR templates",
      "expected": "[e2e] kubectl exec bao policy read push-app-credentials confirms +/+/admin/app-credential globs and absence of the literal leaf",
      "requirement_id": "REQ-005"
    },
    {
      "test_file": "deploy/openbao/policies/push-app-credentials.hcl",
      "test_function": "review:least-privilege-app-cred",
      "story": "Security reviewer confirms OpenBao ACLs stay least-privilege under the per-CR templates",
      "expected": "[review] Manual/grep check: no kv-v2/.../keystone/* trailing wildcard; /admin/app-credential suffix retained; CC-0112 header",
      "requirement_id": "REQ-005"
    },
    {
      "test_file": "deploy/openbao/policies/push-keystone-admin.hcl",
      "test_function": "review:least-privilege-keystone-admin",
      "story": "Security reviewer confirms OpenBao ACLs stay least-privilege under the per-CR templates",
      "expected": "[review] grep confirms bootstrap/+/+/admin (data+metadata, CRUD), no trailing-* over bootstrap, single-CR header removed",
      "requirement_id": "REQ-006"
    },
    {
      "test_file": "tests/e2e/c5c3/multi-controlplane/chainsaw-test.yaml",
      "test_function": "step:verify-bootstrap-sibling-protected",
      "story": "Security reviewer confirms OpenBao ACLs stay least-privilege under the per-CR templates",
      "expected": "[e2e] A token bound to push-keystone-admin cannot write kv-v2/data/bootstrap/infrastructure/mariadb (denied)",
      "requirement_id": "REQ-006"
    },
    {
      "test_file": "deploy/openbao/policies/push-keystone-keys.hcl",
      "test_function": "review:fernet-credential-two-segment-glob",
      "story": "Security reviewer confirms OpenBao ACLs stay least-privilege under the per-CR templates",
      "expected": "[review] grep confirms +/+/{fernet,credential}-keys on data+metadata and the /db exclusion paragraph still holds",
      "requirement_id": "REQ-007"
    },
    {
      "test_file": "tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml",
      "test_function": "step:assert-per-cr-fernet-credential-paths",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[e2e] Update the four asserted kv-v2 paths to the namespace-segmented openstack/keystone/{ns}/{name}/{fernet,credential}-keys shape",
      "requirement_id": "REQ-007"
    },
    {
      "test_file": "deploy/openbao/policies/eso-management.hcl",
      "test_function": "review:read-coverage-per-cr",
      "story": "Security reviewer confirms OpenBao ACLs stay least-privilege under the per-CR templates",
      "expected": "[review] Verify kv-v2/data/openstack/keystone/* and kv-v2/data/bootstrap/* cover the new shapes on read; widen + comment if not; stays read-only",
      "requirement_id": "REQ-008"
    },
    {
      "test_file": "tests/e2e/c5c3/multi-controlplane/chainsaw-test.yaml",
      "test_function": "step:verify-eso-materialises-per-cr-secret",
      "story": "Operator engineer runs two ControlPlanes in two namespaces without OpenBao collisions",
      "expected": "[e2e] Both per-CR k-orc clouds.yaml Secrets materialise (no 403 on read), proving eso-management read coverage",
      "requirement_id": "REQ-008"
    },
    {
      "test_file": "deploy/openbao/bootstrap/write-bootstrap-secrets.sh",
      "test_function": "review:bootstrap-per-cr-loop",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[review] shellcheck-clean; KORC_CONTROLPLANES loop seeds per-CR paths + mark_eso_managed; no unconditional legacy flat seed remains; set -euo pipefail kept",
      "requirement_id": "REQ-009"
    },
    {
      "test_file": "tests/e2e/c5c3/full-controlplane-keystone/chainsaw-test.yaml",
      "test_function": "step:bootstrap-seeds-default-identity",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[e2e] Default (no KORC_CONTROLPLANES override) run seeds the canonical per-CR admin AC + bootstrap/admin paths and the single-CR deploy still reaches Ready",
      "requirement_id": "REQ-009"
    },
    {
      "test_file": "operators/c5c3/api/v1alpha1/controlplane_webhook_test.go",
      "test_function": "TestValidateCreate_RejectsSecondControlPlaneInNamespace",
      "story": "Cluster admin is prevented from creating a second ControlPlane in one namespace",
      "expected": "[unit] Seed a fake client with one existing ControlPlane in ns; ValidateCreate of a second returns an error naming the existing CR",
      "requirement_id": "REQ-010"
    },
    {
      "test_file": "operators/c5c3/api/v1alpha1/controlplane_webhook_test.go",
      "test_function": "TestValidateCreate_AllowsFirstControlPlane_AndUpdate",
      "story": "Cluster admin is prevented from creating a second ControlPlane in one namespace",
      "expected": "[unit] Empty-namespace create is admitted; ValidateUpdate of an existing CR does not run the count check",
      "requirement_id": "REQ-010"
    },
    {
      "test_file": "operators/c5c3/internal/controller/reconcile_credentialrotation_test.go",
      "test_function": "TestResolveControlPlane_AmbiguousIsDefenseInDepth",
      "story": "Cluster admin is prevented from creating a second ControlPlane in one namespace",
      "expected": "[unit] Two CPs in a namespace still yield Reason AmbiguousControlPlane; comment/test documents it as an unreachable-with-webhook fallback",
      "requirement_id": "REQ-010"
    },
    {
      "test_file": "deploy/eso/externalsecrets/keystone-admin.yaml",
      "test_function": "review:reader-matches-writer-password",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[review] remoteRef.key == per-CR bootstrap/{ns}/{keystone}/admin for the deployment identity; property stays password",
      "requirement_id": "REQ-011"
    },
    {
      "test_file": "deploy/eso/externalsecrets/k-orc-clouds-yaml.yaml",
      "test_function": "review:reader-matches-writer-appcred",
      "story": "Operator engineer runs two ControlPlanes in two namespaces without OpenBao collisions",
      "expected": "[review] both ExternalSecrets' remoteRef.key == openstack/keystone/{ns}/{cp}/admin/app-credential for the default identity; #412-supersedes comment present",
      "requirement_id": "REQ-011"
    },
    {
      "test_file": "tests/e2e/c5c3/multi-controlplane/chainsaw-test.yaml",
      "test_function": "step:two-controlplanes-ready-distinct-ac-id",
      "story": "E2E suite proves two ControlPlanes are isolated end-to-end",
      "expected": "[e2e] Apply CPs in tenant-a/tenant-b; assert both Ready=True with distinct status.adminApplicationCredential.id",
      "requirement_id": "REQ-012"
    },
    {
      "test_file": "tests/e2e/c5c3/multi-controlplane/chainsaw-test.yaml",
      "test_function": "step:rotate-one-ac-other-unaffected",
      "story": "E2E suite proves two ControlPlanes are isolated end-to-end",
      "expected": "[e2e] Rotate tenant-a AC via CredentialRotation; assert tenant-b lastRotation unchanged and AC Available=True throughout",
      "requirement_id": "REQ-012"
    },
    {
      "test_file": "operators/c5c3/internal/controller/integration_test.go",
      "test_function": "TestIntegration_MultiControlPlane_DistinctAdminCredentialPaths",
      "story": "E2E suite proves two ControlPlanes are isolated end-to-end",
      "expected": "[integration] envtest brings up two ControlPlanes (different namespaces); asserts distinct PushSecret RemoteKeys and distinct User names",
      "requirement_id": "REQ-012"
    },
    {
      "test_file": "tests/e2e/c5c3/full-controlplane-keystone/chainsaw-test.yaml",
      "test_function": "step:happy-path-per-cr-key",
      "story": "E2E suite proves two ControlPlanes are isolated end-to-end",
      "expected": "[e2e] Any direct OpenBao-path sub-assert updated to openstack/keystone/openstack/controlplane/admin/app-credential; CP reaches Ready=True",
      "requirement_id": "REQ-013"
    },
    {
      "test_file": "operators/c5c3/internal/controller/integration_test.go",
      "test_function": "TestIntegration_SingleControlPlane_AdminCredentialReady",
      "story": "E2E suite proves two ControlPlanes are isolated end-to-end",
      "expected": "[integration] Existing single-CP envtest still drives AdminCredentialReady=True with the per-CR RemoteKey",
      "requirement_id": "REQ-013"
    },
    {
      "test_file": "docs/reference/c5c3/controlplane-reconciler.md",
      "test_function": "review:per-cr-and-multi-instance-section",
      "story": "Operator and reference docs describe the per-CR layout and drop the single-CR caveats",
      "expected": "[review] Lines ~507/519/529/641 show per-CR keys; AmbiguousControlPlane row updated; new Multi-instance section present",
      "requirement_id": "REQ-014"
    },
    {
      "test_file": "docs/reference/keystone/keystone-reconciler.md",
      "test_function": "review:model-b-table-per-cr",
      "story": "Operator and reference docs describe the per-CR layout and drop the single-CR caveats",
      "expected": "[review] Model B PushSecret table (~2728-2744) shows bootstrap/{ns}/{name}/admin; single-CR caveat removed",
      "requirement_id": "REQ-014"
    },
    {
      "test_file": "docs/reference/infrastructure/infrastructure-manifests.md",
      "test_function": "review:policy-doc-per-cr-template",
      "story": "Operator and reference docs describe the per-CR layout and drop the single-CR caveats",
      "expected": "[review] Lines ~687/699/704-705 describe the +/+ templates and per-CR read keys",
      "requirement_id": "REQ-014"
    },
    {
      "test_file": "docs/reference/c5c3/controlplane-reconciler.md",
      "test_function": "review:migration-section-present",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[review] Migration section gives bao kv get->put copy, ACL re-apply, upgrade order, and bao kv metadata delete purge; mirrors CC-0093 wording",
      "requirement_id": "REQ-015"
    },
    {
      "test_file": "docs/reference/keystone/keystone-reconciler.md",
      "test_function": "review:migration-note-admin-password",
      "story": "SRE migrates an existing single-tenant cluster onto the per-CR layout",
      "expected": "[review] keystone-reconciler.md migration note extended to cover bootstrap/keystone-admin -> bootstrap/{ns}/{name}/admin and the boundary-4 fernet/credential copy",
      "requirement_id": "REQ-015"
    },
    {
      "test_file": ".planwerk (PR description)",
      "test_function": "review:no-submodule-edit-upstream-flagged",
      "story": "Operator and reference docs describe the per-CR layout and drop the single-CR caveats",
      "expected": "[review] git submodule status shows architecture/ unchanged; PR description flags the architecture/docs/09-implementation update as a separate upstream PR",
      "requirement_id": "REQ-016"
    },
    {
      "test_file": "docs/reference/c5c3/controlplane-reconciler.md",
      "test_function": "review:architecture-deviation-recorded-not-edited",
      "story": "Operator and reference docs describe the per-CR layout and drop the single-CR caveats",
      "expected": "[review] Any architecture/docs/09-implementation flat-path mention is recorded as a deferred upstream change (not edited from this worktree); git submodule status shows architecture/ unchanged",
      "requirement_id": "REQ-016"
    }
  ],
  "affected_files": [],
  "similar_patterns": [],
  "review_criteria": [
    "[blocker] adminAppCredentialRemoteKeyFor(cp) returns openstack/keystone/{cp.Namespace}/{cp.Name}/admin/app-credential and the package-level constant is gone (REQ-001) — verify: grep -n adminAppCredentialRemoteKey operators/c5c3/internal/controller/reconcile_korc.go",
    "[blocker] adminPasswordPushSecret RemoteKey == bootstrap/{keystone.Namespace}/{keystone.Name}/admin and no \"bootstrap/keystone-admin\" literal remains in operators/keystone (REQ-002) — verify: grep -rn 'bootstrap/keystone-admin' operators/keystone",
    "[blocker] adminUserRef(cp) == cp.Name+\"-user-admin\" while ensureKORCAdminImports keeps User.Spec.Import.Filter.Name = OpenStackName('admin') (REQ-003)",
    "[blocker] DECISION boundary 4 = option (a): fernet/credential RemoteKeys embed keystone.Namespace and a DECISION comment block is present at reconcile_fernet.go:455 and reconcile_credential.go:459 (REQ-004)",
    "[blocker] push-app-credentials.hcl grants kv-v2/{data,metadata}/openstack/keystone/+/+/admin/app-credential and nothing wider; push-keystone-admin.hcl grants kv-v2/{data,metadata}/bootstrap/+/+/admin; push-keystone-keys.hcl uses +/+/{fernet,credential}-keys (REQ-005,006,007) — verify: grep -n path deploy/openbao/policies/push-*.hcl",
    "[blocker] No write wildcard regression: no kv-v2/.../keystone/* or kv-v2/bootstrap/* trailing-* write grant; /db and sibling-bootstrap lock-outs still hold (REQ-005,006,007)",
    "[blocker] DECISION boundary 6 = option (a): ControlPlaneWebhook.ValidateCreate rejects a second ControlPlane per namespace via w.Client.List; ValidateUpdate does not; resolveControlPlane keeps AmbiguousControlPlane as defense-in-depth; NO CRD schema/byte change (REQ-010) — verify: git diff --stat operators/c5c3/config/crd",
    "[blocker] Static ESO readers agree with the new writers for the default identity: keystone-admin.yaml -> bootstrap/{ns}/{keystone}/admin, k-orc-clouds-yaml.yaml -> openstack/keystone/{ns}/{cp}/admin/app-credential (REQ-011)",
    "[blocker] write-bootstrap-secrets.sh seeds per-CR paths via a KORC_CONTROLPLANES loop and no longer unconditionally seeds the legacy flat paths (REQ-009) — verify: grep -n 'keystone/admin/app-credential\\|bootstrap/keystone-admin' deploy/openbao/bootstrap/write-bootstrap-secrets.sh",
    "[critical] tests/e2e/c5c3/multi-controlplane/ asserts two ControlPlanes Ready=True with distinct status.adminApplicationCredential.id and rotation isolation; full-controlplane-keystone still passes against the per-CR layout (REQ-012, REQ-013)",
    "[critical] Unit tests updated at every pinned literal (reconcile_korc_test.go lines incl 485-486, passwordrotation_test.go:450, integration_test.go:4637, fernet/credential _test.go) and new per-CR/distinctness tests added; envtest two-ControlPlane test present (REQ-001..004, REQ-012)",
    "[critical] eso-management.hcl read coverage verified (or widened) for the new per-CR shapes and remains read-only (REQ-008)",
    "[critical] Docs (controlplane-reconciler.md, controlplane-crd.md, keystone-reconciler.md, infrastructure-manifests.md) show the per-CR layout, a Multi-instance section, and the updated AmbiguousControlPlane row; single-CR wording removed from code comments, policy headers and docs (REQ-014)",
    "[critical] Migration runbook present with bao kv get->put copy, ACL re-apply, upgrade order, and bao kv metadata delete purge, mirroring CC-0093 (REQ-015)",
    "[minor] All new/changed code, comments and policy headers carry the CC-0112 feature id alongside any existing CC-0110/CC-0109/CC-0093 citations (rule 11)",
    "[minor] architecture/ submodule is untouched (git submodule status clean) and the upstream architecture PR is flagged in the PR description (REQ-016) — verify: git submodule status"
  ],
  "implementation_notes": "## Architecture summary\n\nCC-0112 extends the proven CC-0093 per-CR RemoteKey pattern from the fernet/credential\nleaves to the remaining cluster-flat OpenBao identifiers. It is a *path-shape* change plus\nits authorising ACLs, its readers, its seed, an admission guard, and a docs/migration sweep.\nThere is NO new CRD field and NO new API version (boundary 6 option (a) is code-only).\n\n### The four collision sites and their fixes\n1. Admin AC path (c5c3): const -> builder `openstack/keystone/{cp.Namespace}/{cp.Name}/admin/app-credential`.\n2. Admin password path (keystone): literal -> `bootstrap/{keystone.Namespace}/{keystone.Name}/admin`.\n3. K-ORC User import name (c5c3): `adminUserRef` -> `cp.Name + \"-user-admin\"` (K8s name only; OpenStack filter stays 'admin').\n4. Fernet/credential paths (keystone): add a namespace segment -> `openstack/keystone/{ns}/{name}/{fernet,credential}-keys`.\n\n### Recorded Decisions (one option each, per the issue's acceptance criteria)\n- Boundary 4 -> option (a): add the namespace segment (consistent with boundaries 1-2; the\n  one-per-namespace ControlPlane guard does NOT give cluster-wide Keystone-name uniqueness, so a\n  name-uniqueness admission guard (option b) would be a broader, cross-cutting change). Policy widens +/ to +/+/.\n- Boundary 5 -> option (a): parametrise write-bootstrap-secrets.sh with a KORC_CONTROLPLANES\n  identity list (default = the single canonical identity). Keeps the cluster-bootstrap path,\n  is independent of #412, and removes the legacy flat seed. Option (b) (operator-owned seeding)\n  is the natural endpoint but overlaps #412 and is deferred.\n- Boundary 6 -> option (a): ControlPlaneWebhook.ValidateCreate enforces one ControlPlane per\n  namespace using the already-injected (but currently unused) Client; CredentialRotation keeps the\n  AmbiguousControlPlane branch as a defense-in-depth fallback. resolveControlPlane already lists\n  client.InNamespace and assumes exactly one CP per namespace, so this codifies an existing\n  assumption with zero CRD churn. Option (b) (add ControlPlaneRef to CredentialRotationSpec) is a\n  CRD schema addition and is rejected as higher-risk; the multi-instance story is per-namespace.\n- Boundary 7: #414 owns the WRITERS (PushSecret RemoteKeys + policy + seed + User name) and, to\n  satisfy \"the single-CR happy path must not regress\", repoints the STATIC readers to the default\n  identity (keystone-admin.yaml per AC2; k-orc-clouds-yaml.yaml as an interim shim). #412 replaces\n  the static k-orc-clouds-yaml.yaml with an operator-owned, per-CR-templated ExternalSecret in\n  childNamespace(cp). The two issues ship together; this plan stays green standalone.\n\n## Data flow (changed segment highlighted)\n\n```\nControlPlane(cp, ns=N, name=C)\n  reconcileKORC -> ensureKORCAdminImports\n     Domain  metadata.name = C-domain-default            (unchanged, already per-CR)\n     User    metadata.name = C-user-admin   <<< CHANGED (was cloudName 'admin')\n             Spec.Import.Filter.Name = 'admin'           (unchanged OpenStack name)\n  reconcileAdminCredential -> adminAppCredentialPushSecret(cp)\n     RemoteKey = openstack/keystone/N/C/admin/app-credential   <<< CHANGED (was flat)\n       -> openbao-cluster-store (ClusterSecretStore) -> OpenBao KV-v2\n            authorised by push-app-credentials.hcl: +/+ /admin/app-credential   <<< CHANGED\n\nKeystone(ks, ns=N, name=K) [projected as C-keystone, or standalone 'keystone']\n  reconcilePasswordRotation -> adminPasswordPushSecret(ks)\n     RemoteKey = bootstrap/N/K/admin                          <<< CHANGED (was bootstrap/keystone-admin)\n       authorised by push-keystone-admin.hcl: bootstrap/+/+/admin            <<< CHANGED\n  reconcileFernet/Credential\n     RemoteKey = openstack/keystone/N/K/{fernet,credential}-keys             <<< CHANGED (added N)\n       authorised by push-keystone-keys.hcl: +/+/{fernet,credential}-keys    <<< CHANGED\n\nReaders (static, repointed for the default identity; #412 makes k-orc per-CR):\n  keystone-admin.yaml         key = bootstrap/N/K/admin\n  k-orc-clouds-yaml.yaml (x2) key = openstack/keystone/N/C/admin/app-credential\n\nAdmission: ControlPlaneWebhook.ValidateCreate List(InNamespace) -> reject 2nd CP per ns.\n```\n\n## Patterns to follow (from the codebase)\n- Builder-returns-RemoteKey: reconcile_fernet.go:455 / reconcile_credential.go:459 (CC-0093).\n- cp.Name-prefixed child name: adminDomainRef (reconcile_korc.go:683-685), keystoneName (reconcile_keystone.go:41).\n- +-glob policy + upstream citation + /db lock-out: push-keystone-keys.hcl:14-87 (reuse verbatim shape for +/+).\n- Metadata-path-must-be-granted rationale: present in all three push-*.hcl headers (preserve it).\n- Migration note discipline: keystone-reconciler.md:863-902 (\"orphaned but harmless\", bao kv metadata delete, re-apply ACL, order vs upgrade).\n- Decision-comment format: \"DECISION (CC-0112, boundary N): <unclear> -> chose <X> because <why>. Reviewer: please verify.\"\n- Webhook injected Client: ControlPlaneWebhook.Client (controlplane_webhook.go:48) already present, mirrors KeystoneWebhook.\n\n## Default identity (for the static-reader repoint and the bootstrap default)\ndeploy/kind/controlplane/controlplane.yaml: ControlPlane name=controlplane, namespace=openstack;\nprojected Keystone = controlplane-keystone (reconcile_keystone.go keystoneName = cp.Name+\"-keystone\").\n=> default admin AC path:        openstack/keystone/openstack/controlplane/admin/app-credential\n=> default admin password path:  bootstrap/openstack/controlplane-keystone/admin\n=> default fernet/credential:    openstack/keystone/openstack/controlplane-keystone/{fernet,credential}-keys\nVerify these against the actual e2e fixtures before baking the literals (standalone keystone Quick\nStart uses Keystone name 'keystone', so its keystone-admin reader resolves to bootstrap/openstack/keystone/admin).\n\n## Pitfalls\n- Do NOT change CloudCredentialsRef.SecretName ('k-orc-clouds-yaml') or cloudName ('admin') defaults — out of scope.\n- The User change is the K8s object name ONLY; flipping Spec.Import.Filter.Name would break K-ORC's import of the real OpenStack admin user.\n- The one-per-namespace check belongs in ValidateCreate, not the shared validate() (a List in ValidateUpdate would see the object itself / churn on every update).\n- +-glob matches exactly one path segment; +/+ requires both the namespace AND name segments — verify with `bao policy read` in the e2e, not by inspection alone.\n- keystone-admin.yaml is shared between the standalone-keystone and controlplane Quick Starts whose Keystone CRs have different names; a single static literal can serve only one — this is the concrete driver for the operator-owned templating tracked in #412.\n- architecture/ is a submodule: READ-ONLY from this worktree (CRITICAL rule 12). The architecture-doc change is a separate upstream PR (REQ-016), matching CC-0093 REQ-008.\n\n## Deferred items (explicitly OUT of scope for CC-0112)\n- #412: operator-owned, per-CR-templated k-orc-clouds-yaml ExternalSecret (and optional removal of the orc-system global copy). This plan only repoints the static manifest as an interim shim. Revisit: ship alongside this PR.\n- Boundary 5 option (b): moving the OpenBao seed responsibility into the c5c3-operator. Revisit once #412 lands (operator becomes the single source of truth for seed state).\n- Boundary 6 option (b): a ControlPlaneRef field on CredentialRotationSpec for genuine multiple-CPs-per-namespace. Revisit only if the per-namespace tenancy model is rejected.\n- Scheduled CredentialRotation loop (intervalDays/preRotationDays/gracePeriodDays) stays read-but-ignored (reconcile_credentialrotation.go:96-110) — unchanged.\n- Automatic backfill of legacy OpenBao data — migration is operator-driven via the runbook (REQ-015); the operator never reads/copies legacy leaves.\n- architecture/docs/09-implementation update — separate upstream PR (REQ-016).\n\n## Observability / safety\n- Conditions: AdminCredentialReady (c5c3), FernetKeysReady/CredentialKeysReady and the Model-B\n  password-rotation condition (keystone) are the existing signals; a 403 from a too-narrow ACL\n  flips them False with a clear message — the migration note's \"re-apply the ACL\" step is the fix.\n- The webhook guard converts a silent last-writer-wins clobber (surfaced only post-hoc as\n  AmbiguousControlPlane) into a clean admission-time rejection.",
  "status_history": {
    "draft": {
      "github_account": "berendt",
      "timestamp": "2026-06-05T18:31:06.252869"
    },
    "preparing": {
      "github_account": "berendt",
      "timestamp": "2026-06-05T18:31:06.648569"
    },
    "prepared": {
      "github_account": "berendt",
      "timestamp": "2026-06-05T18:56:12.996043"
    }
  },
  "execution_history": [
    {
      "run_id": "0509a2ac-0a0c-44ae-a746-3485d300332a",
      "timestamp": "2026-06-05T18:56:12.996078",
      "total_duration": 1504.116595506668,
      "status": "completed",
      "timings": [
        {
          "name": "prepare",
          "duration": 1504.116595506668,
          "type": "prepare",
          "status": "done"
        }
      ]
    }
  ]
}