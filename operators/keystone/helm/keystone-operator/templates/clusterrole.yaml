{{- if not .Values.rbac.namespaceScoped }}
# NOTE: Rules MUST be kept in sync with role.yaml (CC-0043).
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "operator-library.fullname" . }}
  labels:
    {{- include "operator-library.labels" . | nindent 4 }}
rules:
  {{- include "keystone-operator.rbacRules" . | nindent 2 }}
{{- end }}
