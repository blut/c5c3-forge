# Release namespace unit tests.
#
# Verifies that the glance-operator Helm chart correctly threads
# .Release.Namespace through every namespaced resource, the
# cert-manager.io/inject-ca-from annotation on the webhook configurations,
# and the ClusterRoleBinding subjects entry — when installed with
# --namespace glance-system (the dedicated controller Namespace).
#
# These assertions are namespace-parametric: the release namespace is set
# once at the suite level via `release.namespace` and the expected value is
# `glance-system` in every assertion. Existing test suites in this chart
# rely on the helm-unittest default release namespace (NAMESPACE) and
# remain untouched.
suite: Release Namespace
release:
  name: glance-operator
  namespace: glance-system
templates:
  - templates/certificate.yaml
  - templates/clusterrolebinding.yaml
  - templates/deployment.yaml
  - templates/networkpolicy.yaml
  - templates/rolebinding.yaml
  - templates/service.yaml
  - templates/serviceaccount.yaml
  - templates/servicemonitor.yaml
  - templates/webhook-configuration.yaml
tests:
  # ---------------------------------------------------------------------------
  # Namespaced resources rendered with default values (webhook on, monitoring
  # off, namespaceScoped off, networkPolicy off).
  # ---------------------------------------------------------------------------
  - it: should render the Deployment in glance-system
    template: templates/deployment.yaml
    asserts:
      - equal:
          path: metadata.namespace
          value: glance-system

  - it: should render the ServiceAccount in glance-system
    template: templates/serviceaccount.yaml
    asserts:
      - equal:
          path: metadata.namespace
          value: glance-system

  - it: should render the Service in glance-system
    template: templates/service.yaml
    asserts:
      - equal:
          path: metadata.namespace
          value: glance-system

  - it: should render the self-signed Issuer in glance-system
    template: templates/certificate.yaml
    documentIndex: 0
    asserts:
      - isKind:
          of: Issuer
      - equal:
          path: metadata.namespace
          value: glance-system

  - it: should render the webhook Certificate in glance-system
    template: templates/certificate.yaml
    documentIndex: 1
    asserts:
      - isKind:
          of: Certificate
      - equal:
          path: metadata.namespace
          value: glance-system

  - it: should reference glance-system in the Certificate SAN DNS names
    template: templates/certificate.yaml
    documentIndex: 1
    asserts:
      - contains:
          path: spec.dnsNames
          content: glance-operator.glance-system.svc
      - contains:
          path: spec.dnsNames
          content: glance-operator.glance-system.svc.cluster.local

  # ---------------------------------------------------------------------------
  # Webhook configurations are cluster-scoped, but the inject-ca-from
  # annotation MUST point to the release namespace so cert-manager finds the
  # Certificate object emitted into glance-system.
  # ---------------------------------------------------------------------------
  - it: should set inject-ca-from to glance-system/<fullname>-webhook on the MutatingWebhookConfiguration
    template: templates/webhook-configuration.yaml
    documentIndex: 0
    asserts:
      - isKind:
          of: MutatingWebhookConfiguration
      - equal:
          path: metadata.annotations["cert-manager.io/inject-ca-from"]
          value: glance-system/glance-operator-webhook
      - equal:
          path: webhooks[0].clientConfig.service.namespace
          value: glance-system

  - it: should set inject-ca-from to glance-system/<fullname>-webhook on the ValidatingWebhookConfiguration
    template: templates/webhook-configuration.yaml
    documentIndex: 1
    asserts:
      - isKind:
          of: ValidatingWebhookConfiguration
      - equal:
          path: metadata.annotations["cert-manager.io/inject-ca-from"]
          value: glance-system/glance-operator-webhook
      - equal:
          path: webhooks[0].clientConfig.service.namespace
          value: glance-system

  # ---------------------------------------------------------------------------
  # ClusterRoleBinding is cluster-scoped but binds a ServiceAccount that lives
  # in the release namespace — subjects[0].namespace MUST be glance-system.
  # ---------------------------------------------------------------------------
  - it: should bind to the ServiceAccount in glance-system
    template: templates/clusterrolebinding.yaml
    asserts:
      - isKind:
          of: ClusterRoleBinding
      - equal:
          path: subjects[0].kind
          value: ServiceAccount
      - equal:
          path: subjects[0].namespace
          value: glance-system

  # ---------------------------------------------------------------------------
  # Conditionally-rendered namespaced resources.
  # ---------------------------------------------------------------------------
  - it: should render the ServiceMonitor in glance-system when enabled
    template: templates/servicemonitor.yaml
    set:
      monitoring:
        serviceMonitor:
          enabled: true
    asserts:
      - isKind:
          of: ServiceMonitor
      - equal:
          path: metadata.namespace
          value: glance-system

  - it: should render the NetworkPolicy in glance-system when enabled
    template: templates/networkpolicy.yaml
    set:
      networkPolicy:
        enabled: true
        kubeApiServer:
          cidrs: ["10.96.0.1/32"]
          ports: [6443]
    asserts:
      - isKind:
          of: NetworkPolicy
      - equal:
          path: metadata.namespace
          value: glance-system

  - it: should render the RoleBinding in glance-system in namespace-scoped mode
    template: templates/rolebinding.yaml
    set:
      rbac:
        namespaceScoped: true
      webhook:
        enabled: false
    asserts:
      - isKind:
          of: RoleBinding
      - equal:
          path: metadata.namespace
          value: glance-system
      - equal:
          path: subjects[0].namespace
          value: glance-system
